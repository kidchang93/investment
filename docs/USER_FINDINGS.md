# USER_FINDINGS

> 실제 주문자(개인 투자자) 관점으로 앱을 처음부터 끝까지 써보며 발견한 것을 날짜별로 남긴다.
> 코드 리뷰가 아니라 사용 관점 기록이므로, `docs/ARCHITECTURE.md`·`docs/DESIGN.md`·`docs/CODE_STYLE.md`의
> 기준과 충돌하는 부분은 이 문서가 지적하는 쪽이고 저 문서들이 규범이다.

## 2026-07-27 장중 사용자 관점 점검

### 요약

- **가장 큰 오해를 바로잡음**: 사전 브리핑에는 "지금 앱에 호가창이 없다"고 되어 있었지만, 실제로는 `GET /api/instruments/:id/order-book`가 살아 있고 프런트 `OrderBookPanel`(주문 티켓 맨 위)이 10단계 매도/매수 호가·잔량·동시호가 예상체결·VI(변동성완화장치) 알림까지 렌더링한다. 호가 행을 누르면 지정가 칸에 그 가격이 그대로 들어간다. 이건 고칠 필요 없는 기능이니 다음 점검자는 "호가창이 없다"는 전제로 재조사하지 않아도 된다.
- 실제로 걸린 가장 위험한 지점은 **수동 실계좌 주문 티켓이 자동매매 패널과 달리 `시장가 허용`·`허용/차단 종목` 리스크 룰을 주문 확인 화면 이전에 검사하지 않는 것**이다. 주문 티켓을 열면 기본값이 이미 리스크 룰로 막혀 있는 `시장가`인데도, 확인 단계는 "실계좌 주문입니다. 확인하면 그대로 접수됩니다"라고 말한다(실제로는 서버가 막는다).
- 종목 화면에 처음 들어가면 선택 종목이 진짜 삼성전자가 아니라 주문이 안 되는 "삼성전자 야간 환산가"(NIGHT_PROXY)로 잡혀 있어, 초심자는 이 가격이 지금 살 수 있는 가격인 줄 착각할 수 있다.
- 주문 전 화면 어디에도 수수료·세금 예상액이 없다. `발견 > 도구 > 수수료` 계산기는 있지만 주문 금액과 연결되어 있지 않다.
- 실계좌 예수금이 50,000원뿐이라 삼성전자는 1주도 살 수 없는 상태였다(`실계좌 매수가능 49,751원, 최대 0주` — 실제 API·화면에서 확인). 그래서 "10주 매수 → 체결 확인 → 취소" 흐름은 자금 부족으로 끝까지 실습하지 못했다(아래 "확인하지 못한 것" 참고).

### 요구사항 (우선순위 순)

| # | 요구사항 | 왜 필요한가 (실제 시나리오) | 지금은 어떻게 되어 있나 | 근거(파일·API·화면) |
|---|---|---|---|---|
| 1 | 실계좌 수동 주문 티켓도 자동매매 패널처럼 `주문 확인` 단계 이전에 `시장가 허용`·`허용 종목`·`차단 종목` 리스크 룰을 미리 검사해 버튼을 잠그고 사유를 보여줘야 한다 | 지금 계좌(21)의 리스크 룰은 `allowMarketOrder:false`인데 주문 티켓을 열면 주문유형 기본값이 `시장가 — 지금 값에 바로`다. 이 상태로 수량만 채우고 `매수 주문하기`를 누르면 "실계좌 주문입니다. 확인하면 그대로 접수됩니다"라는, 사실이 아닌 안내를 보고 확인 단계까지 간다. 실제로는 서버 리스크 룰이 막아 주문이 안 나가지만, 사용자는 "확인하면 접수된다"는 말을 믿고 눌렀다가 막판에야 막힌 이유를 알게 된다 | 자동매매 패널의 `autoTraderBlockers`(frontend/src/App.tsx:3734-3770)는 `riskRules.allowMarketOrder`·`riskRules.symbolAllowlist`를 미리 읽어 "시장가 주문이 막혀 있습니다 … 설정으로 이동" 식으로 버튼을 잠그고 이동 링크까지 준다. 반면 수동 주문 티켓의 `liveOrderBlockers`(frontend/src/App.tsx:3525-3548)는 게이트 상태·종목 가능 여부·수량>0·지정가>0만 보고 `riskRules`를 아예 참조하지 않는다(의존성 배열에 `riskRules`가 없음). 서버의 `evaluateLiveOrderGate()`(backend/src/server.ts:125-140)도 `KIS_LIVE_ORDER_ENABLED`와 등록 계좌 수만 보고 리스크 룰은 실제 주문 제출(`POST /api/broker/kis/orders`) 시점에만 검사한다(`backend/src/db/riskRules.ts:200` `if (input.orderType === 'market' && !rules.allowMarketOrder)`). `docs/TRADING_API.md`의 "열리기 위한 조건" 5번(`리스크 룰 통과`)이 게이트 조건처럼 적혀 있지만, 실제 `live-order-gate` 응답(`curl`로 확인, `{"blockers":["실주문이 꺼져 있습니다…"]}`)에는 리스크 룰 위반이 전혀 반영되지 않는다 — 문서와 구현이 어긋나 있다 |
| 2 | `종목` 화면 진입 시 기본 선택 종목이 실제 KRX 종목(예: 005930)이어야 하고, 최소한 주문 불가 종목(야간 환산가·선물·원자재)을 기본값으로 띄우지 않아야 한다 | 앱을 처음 열고 `종목` 탭을 누르면 선택된 종목이 "005930-NIGHT · 삼성전자 야간 환산가"였다. 이 화면은 `장 상태: GDR 장마감`, `정규장 08:00-16:30`(런던 GDR 시간), 가격 246,365원처럼 실제 KRX 정규장과 무관한 값을 크게 보여준다. 초심자는 이게 지금 살 수 있는 삼성전자 가격인 줄 알고 주문 폼을 채우다가 `국내 주식·ETF·ETN만 주문할 수 있습니다` 안내를 보고서야 잘못된 종목이었음을 안다 | 실측 스크린샷: 새 탭에서 `종목` 클릭 시 상단 종목명이 "삼성전자 야간 환산가"로 표시되고 주문 티켓 대신 `UnorderableInstrumentNotice`가 뜬다. `삼성전자` 탭을 직접 눌러야 진짜 KOSPI 종목으로 바뀐다. `shared/src/index.ts`의 `Instrument.assetType`에 `NIGHT_PROXY`가 있고 `backend/src/server.ts` 호가 라우트 주석에도 "야간 환산가·원자재·선물은 KRX 호가 대상이 아니다"라고 명시돼 있을 만큼 명백히 주문 대상이 아닌 종목인데, 첫 화면 기본값으로 뜬다 |
| 3 | 주문 티켓에 예상 수수료·세금(또는 최소한 "수수료·세금 별도" 안내와 계산기로 가는 링크)을 붙여야 한다 | "10주 사면 총 얼마 드는지"를 물었을 때 화면은 `예상 단가 × 수량 = 예상 주문액`만 보여준다(예: 10주 × 257,000원 = 2,570,000원). 매도 시 붙는 증권거래세·수수료는 어디에도 안 보인다. 발견 탭에 `수수료 비교 계산기`가 있다는 걸 아는 사용자만 별도로 찾아가 금액을 다시 입력해야 한다 | 주문 티켓 요약 칸은 `예상 단가`·`예상 주문액`·`실계좌 매수가능` 세 칸뿐(frontend/src/App.tsx:6519-6544). 수수료·세금은 매도 체결 이후 `기간별 매매손익` 카드에서만 보인다(frontend/src/App.tsx:5771,5792, `kisTradeProfit.totalFee/totalTax`) — 이미 판 다음에야 실제 수수료를 알 수 있다. `수수료 비교 계산기`(발견 > 도구 > 수수료, frontend/src/App.tsx:4862)는 매수금액을 기본값 1,000,000원으로 직접 타이핑해야 하고 주문 티켓의 실제 금액과 연동되지 않는다 |
| 4 | 실시간 체결이 아직 없을 때(장 시작 직후 등) 큰 가격 숫자 옆에 "이 값은 전일종가입니다 / 방금 값입니다"를 값과 같은 위치·굵기로 붙이고, `Quote` 응답에 조회 시각을 담아야 한다 | 09:00 장 시작 직전(08:59)에 종목 화면을 보면 큰 글씨로 `249,500원 0원 (0.00%)`이 뜨고, 그 값이 실시간인지 전일 종가인지 알려주는 `실시간 대기`라는 글자는 종목명 옆에 훨씬 작게 붙어 있다. 체결이 들어오기 전까지 "이 숫자를 믿어도 되는가? 언제 값인가?"에 화면이 바로 답하지 않는다 | 실측: 08:59 스크린샷에서 `005930 · KOSPI · KRW  삼성전자 실시간 대기` 옆에 `249,500원 0원 (0.00%)`. `shared/src/index.ts:115-125`의 `Quote` 타입에는 `code/price/change/changeRate/sign/open/high/low/accVolume`만 있고 조회 시각 필드가 없다 — 같은 파일의 `OrderBook.fetchedAt`(127번대), `BrokerOrderability.fetchedAt` 등 다른 스냅샷 타입들은 전부 시각을 갖고 있는 것과 대조적이다. 장중 진입 이후에는 체결(`Trade.time`)이 들어와 헤더의 `체결 N초 전`으로 신선도가 보이므로, 문제는 "체결이 아직 없는 짧은 구간"에 한정된다 |
| 5 | (재현율 낮음, 참고용) 상단 탭을 오가는 동안 선택 종목과 사이드바 탭이 예고 없이 초기화되는 경로가 있는지 재현 테스트가 필요하다 | `종목`에서 삼성전자를 고르고 주문 티켓에 수량 10을 입력한 뒤 `내 계좌`로 갔다가 다시 `종목`으로 돌아오니 선택 종목이 "005930-NIGHT"로, 사이드바가 `주문` 탭에서 `탐색` 탭으로 되돌아가 있었다(입력했던 수량도 사라짐). 이후 같은 경로(`내 계좌`↔`종목`, `종목`↔`발견`↔`종목`)를 여러 번 반복했지만 다시 재현되지 않았다 — 어떤 조건에서 발생하는지 특정하지 못했다 | 스크린샷 비교: 수량 10을 입력한 직후 캡처(`삼성전자` 선택, `주문` 탭)와 `내 계좌`를 거쳐 돌아온 직후 캡처("005930-NIGHT" 선택, `탐색` 탭)가 다르다. `selectedInstrument`는 `useState<Instrument | null>(null)`로 로컬 상태이며(frontend/src/App.tsx:1843) localStorage에 저장되지 않으므로, 리렌더 타이밍이나 비동기 종목 목록 갱신과 얽힌 경쟁 조건일 가능성이 있다 — 코드까지는 특정하지 못했다 |
| 6 | 주문 하나의 진행 상태를 한 곳에서 추적할 수 있게, `실계좌 주문 기록`·`실계좌 주문·체결`·`미체결 주문`·`주문 진행 알림` 네 카드를 주문번호로 서로 연결(클릭 시 강조·스크롤)해야 한다 | "주문을 냈다고 치자, 지금 어디까지 진행됐는지 어떻게 아는가?"에 대한 답이 카드 네 개에 흩어져 있다. `실계좌 주문 기록`은 전송 시도(접수/차단/거부) 감사 로그이고, 실제 체결 여부는 `실계좌 주문·체결`(체결/부분체결/미체결/취소/거부)에서 따로 봐야 하며, 진행 중 상태는 `주문 진행 알림`(HTS ID 필요, 소켓 이벤트만 표시)에서, 정정·취소 가능한 건은 `미체결 주문`에서 각각 확인해야 한다. 네 카드 모두 `주문번호`를 갖고 있지만 서로 클릭 연동은 없다 | `내 계좌` 화면 실측: `실계좌 주문·체결`(총 주문수량/체결수량/체결금액/미체결 요약), `실계좌 주문 기록`(16건, 시각·동작·종목·수량단가·주문번호·사유·상태 표), `실계좌 예약주문`(별도 카드) 세 곳이 세로로 나열돼 있고 `종목` 화면의 `체결을 기다리는 주문`(미체결)·`주문 진행 알림`까지 합치면 총 네 곳이다. `docs/DESIGN.md`의 "화면 연결 상태" 표에도 이 네 기능이 각각 다른 행으로 나뉘어 있다 |

### 7. ~~마감 동시호가에 큰 글씨 가격이 멈춘 값인데 화면은 `정규장 거래 중`이라고 한다~~ (2026-07-27 15:26 실측 → 같은 날 고침)

`e7cc40a`에서 호가창은 고쳤다 — 마감 동시호가 코드 `121`을 넣어 `동시호가 예상
체결 …원 · 아직 체결된 값이 아닙니다`가 뜬다. 그런데 **화면 맨 위 큰 글씨와
장 상태는 아직 이 사실을 모른다.**

15:26:18 실측(`000660`):

| 자리 | 화면이 말한 것 | 실제 |
|------|------|------|
| 큰 글씨 가격 | `1,820,000원 +3.47%` (15:19:59에 멈춤) | 동시호가 예상 체결 `1,836,000원 +4.38%` |
| 장 상태 | `정규장 / 거래 중` | 마감 동시호가 (연속 체결 없음) |

**16,000원 차이**를 큰 글씨가 말하지 않는다. 아침 08:51에 봤던 것과 같은
모양이다(`docs/TRADING_API.md` 프리마켓 절).

원인: `marketSession`은 `getMarketSession(selectedInstrument)`으로 **시계만
보고** 정한다(App.tsx:3337). KIS가 주는 `sessionPhase`는 `OrderBookPanel`
안에서만 쓰이고 헤더까지 오지 않는다. 15:20~15:30이 정규장 시간(09:00~15:30)
안이라 시계 기준으로는 `정규장 거래 중`이 맞지만, 그 구간에는 연속 체결이
없고 가격이 굳는다.

**고쳤다.** 호가 응답을 헤더까지 올리는 대신 **시계로 판단**했다 —
`krxAuctionWindow`(shared)가 08:30~08:59와 15:20~15:30을 동시호가로 본다.
호가는 주문 패널이 열려 있을 때만 받고 종목당 호출 1회라, 장 상태 한 줄을
적자고 관심목록 전체에 호출을 붙이면 `EGW00201`에 걸린다. 호출을 늘리지 않고
할 수 있는 만큼만 했다.

지금은 이렇게 적는다:

- 장 상태: `마감 동시호가 · 체결 없이 주문만 모입니다` (장전은 `장전 동시호가`)
- 큰 글씨 옆: `이 값은 마지막 체결가에 멈춰 있습니다 · 예상 체결가는 주문 탭 호가창에서 봅니다`

**아직 안 한 것**: 예상 체결가(1,836,000원) 자체를 헤더에 올리는 것.
그건 호출을 늘려야 해서 비용을 어디서 낼지 정한 뒤에 한다. 지금은 값이
멈췄다는 사실과 어디서 보는지만 말한다.

경계는 시험으로 못 박았다(`backend/src/trading/krxSession.test.ts`, 9개).
15:20 시작은 실측이고(15:20:03에 112→121), **08:30 시작은 KRX 규정에서
가져온 값이라 실측이 아니다** — 관측이 08:53부터라 그 앞을 못 봤다.

### 8. ~~게이트 조회가 실패해도 배지가 `주문 가능`을 계속 주장한다~~ (2026-07-27 실측 → 같은 날 고침)

#6을 검증하다 발견했다. `fetchKisLiveOrderGate`가 실패하면 **받아 둔 값을 지우지
않고** `liveOrderGateError`만 세운다. 코드에 사유가 적혀 있고 의도된 결정이다 —
"지우면 `확인 중`으로 되돌아가 실패가 로딩처럼 보인다"(App.tsx:3062-3066).

그런데 헤더 배지는 `!liveOrderGate ? gateUnknownLabel : ...`이라, **한 번이라도
값을 받은 뒤에는 `확인 실패`가 뜨지 않는다.** 실측: 게이트를 200(enabled:true)으로
한 번 받게 한 뒤 502를 물리니 배지가 `실계좌 주문 가능 · 실전 서버` 그대로였다.
첫 화면 안내 문구도 같은 값을 쓰므로 함께 낡는다.

자동매매 쪽에는 이 문제를 위한 장치가 이미 있다 — `isAutoTraderKnown`
(App.tsx, "받아 둔 값이 있어도 마지막 조회가 실패했으면 `아는` 것이 아니다").
게이트에는 그 짝이 없다.

**고쳤다.** 값을 지우지 않는다는 결정은 그대로 두고 `isGateStale`(값은 있는데
마지막 조회가 실패)을 만들어 **낡았다는 사실만** 덧붙였다.

| 상태 | 배지 | 테두리 | 상태색 |
|------|------|------|------|
| 잠김 | `실계좌 주문 잠김` | 실선 | - |
| 열림 | `실계좌 주문 가능 · 실전 서버` | 실선 | 경고색 |
| 잠김 + 확인 실패 | `실계좌 주문 잠김 · 확인 실패` | 점선 | - |
| 열림 + 확인 실패 | `실계좌 주문 가능 · 실전 서버 · 확인 실패` | 점선 | **경고색 유지** |

마지막 줄이 핵심이다. 낡았다는 이유로 `data-armed`를 `unknown`으로 바꾸면
**열려 있는 게이트가 경고색을 잃는다.** 상태색은 그대로 두고 점선으로만 말한다.
툴팁 둘째 줄과 첫 화면 안내에도 사유가 붙는다.

**차단 자리 네 곳은 일부러 안 건드렸다** - 수동 주문(`liveOrderBlockers`),
정정·취소(`amendCancelBlockers`), 예약주문, 자동매매. 낡은 `열림` 값으로 버튼이
열려 있는 것은 서버가 최종 판정하므로 잘못된 주문이 나가는 게 아니라 403을 듣는
일이고, 특히 **정정·취소에 차단을 더하면 "취소는 위험을 줄이는 동작이라 막으면
안 된다"는 코드의 명시적 결정을 뒤집는다**(`amendCancelBlockers` 위 주석).

## 3차 점검 (2026-07-27 밤)

앱을 다시 띄우려다 막힌 데서 나왔다. 브라우저 확장과 Postgres가 모두 내려가 있어
**화면은 못 보고 코드와 실행 절차만 확인한 회차다** — 화면 확인이 필요한 항목은
그렇게 표시했다.

### 9. ~~실행 절차에 Postgres를 띄우라는 말이 없다~~ (2026-07-27 해소, 명령까지 실측)

`CLAUDE.md`의 "빌드 및 실행"은 `npm install` → `.env` 준비 → `npm run dev` 세
단계다. 그대로 따르면 백엔드가 뜨지 않는다 — 실측:

```
[api] Error: connect ECONNREFUSED 127.0.0.1:55432
[api]   at async ensureInstrumentSchema (backend/src/db/instruments.ts:162:3)
[api]   at async main (backend/src/server.ts:192:3)
```

`DATABASE_URL` 기본값이 `postgresql://kis:kis_local@localhost:55432/kis`인데
**그 DB를 어떻게 띄우는지는 어디에도 없다.** 레포에 compose 파일도 없다.
`npm run dev`는 백엔드가 죽어도 프론트를 계속 띄우므로, 화면은 열리는데 모든
조회가 실패하는 상태가 된다.

**고쳤고, 적어 둔 명령을 실제로 돌려 봤다.** 처음엔 `postgres:16`으로 적었는데
돌고 있는 컨테이너는 `postgres:16-alpine`이었다 — 버리는 컨테이너를 다른 포트로
띄워 `pg_isready`와 `psql`까지 확인하고 이미지 태그를 실제와 맞췄다. 컨테이너가
이미 있으면 `docker start kis-postgres` 한 줄이면 된다는 것도 함께 적었다.

### 10. ~~서버가 죽었을 때 계좌 카드 셋이 `조회하는 중입니다`라고 말한다~~ (2026-07-27 해소)

**처음 적은 전제가 두 군데 틀렸다.** 백엔드를 실제로 내리고(`pkill -f "tsx watch"`)
새로 고쳐 재 보니:

- 전역 배너 문구는 원문(`Failed to fetch`)이 아니었다. `toErrorMessage`가 이미
  **"서버에 연결하지 못했습니다. 백엔드가 실행 중인지 확인하세요."**로 바꿔 준다.
- 배너가 8초 뒤 걷히는 것도 문제가 아니었다. **배너는 아예 뜨지 않았다**
  (2.5초·9.5초 두 번 확인). 왜 안 뜨는지는 아직 못 밝혔다.

**진짜 문제는 계좌 카드 셋이었다.** 서버가 죽어 답이 영영 안 오는데 이렇게 적었다:

| 카드 | 서버가 죽었을 때 |
|------|------|
| KIS 실계좌 | `KIS 계좌 조회 설정을 확인하는 중입니다` |
| 실계좌 주문·체결 | `KIS 체결내역을 조회하는 중입니다` |
| 기간별 매매손익 | `기간별 매매손익을 조회하는 중입니다` |

셋 다 실패해도 상태가 `null`이라 "아직 안 옴"과 구별되지 않았다. **게이트와
리스크 룰은 이미 이 구분을 갖고 있었는데**(`liveOrderGateError`, `riskRulesError`
— "null이 `아직 안 옴`과 `못 받음`을 겸하지 않게 한다") 이 셋만 빠져 있었다.

고쳤다. 이제 `KIS 실계좌를 조회하지 못했습니다 — 서버에 연결하지 못했습니다.
백엔드가 실행 중인지 확인하세요.`라고 적는다. 백엔드를 내린 채로 확인했고, 살린
뒤에는 문구가 사라지고 실제 값(예수금 50,000원)이 나오는 것도 확인했다.

**남은 실마리도 밝혔다.** 자동매매의 `지금 돌고 있는지 확인하는 중입니다`와
채점 성적의 `불러오는 중입니다`는 오류 분기가 **있는데도** 떴는데, 원인은 한
단계 위였다:

```
계좌 목록 조회 실패 → kisAccountId가 null로 남음
  → refreshAutoTrader의 `if (!kisAccountId) return`에 걸려 조회가 시작조차 안 됨
  → autoTraderError는 null → 화면은 "확인하는 중입니다"
```

실패한 것도, 기다리는 것도 아니고 **아예 시작을 못 한 것**이다. 계좌 **목록**
조회에는 전용 오류 상태가 없었고 전역 배너만 세우고 있었는데, 그 배너는 8초 뒤
걷힌다. `kisAccountsError`를 만들어 계좌 id를 전제로 하는 카드들이
`계좌 목록을 불러오지 못해 조회할 수 없습니다 — …`라고 적게 했다.

계좌 **내역**(`kisAccountError`)과 계좌 **목록**(`kisAccountsError`)은 다른
것이라 따로 뒀다.

**전역 배너가 왜 안 뜨는지는 여전히 못 밝혔다.** 백엔드를 내리고 1.5초에 확인해도
없었다(8초 자동 걷힘보다 훨씬 빠른 시점이다). 다만 이제 각 카드가 제 사유를
말하므로 배너가 없어도 화면이 침묵하지는 않는다.

### 11. ~~실계좌 주문 버튼을 여는 판정에 시험이 없었다~~ (같은 회차에 고침)

`riskRuleBlockers`가 빈 배열을 돌려주면 화면의 `매수 주문하기`가 눌린다. 실제
돈이 오가는 자리인데 시험이 없었다 — 서버 쪽 비슷한 판정(`screenQuote`,
`krxSessionKind`, `exitCloseAfter`)은 전부 경계 시험이 있다. 프론트에 테스트
러너가 없어서 생긴 공백이다.

`shared`로 옮겨 backend 러너로 시험한다(`krxAuctionWindow`·`financialPeriodWindow`와
같은 방식). 16개: 룰을 모를 때 3, 통과 2, 경계 3(수량·금액은 **같으면 통과
넘으면 막힘**, NaN은 이 잣대로 안 막음), 종목·유형 6, 그리고 아래 2.

**서버와 비교해 보니 빠진 검사는 결함이 아니었다.** 프론트는 거래 시간대·휴장일·
오늘 누적 한도를 보지 않는데, 그건 `docs/CODE_STYLE.md`의 "서버만 아는 것은 흉내
내지 않는다"를 따른 것이다. **나중에 "빠졌다"고 채우면 서버와 어긋난 순간 화면이
거짓말을 한다** — 그래서 안 본다는 사실 자체를 시험 두 개로 못 박았다(시간대를
00:00~00:01로 좁혀도, 일일 한도를 0으로 둬도 판정이 안 바뀐다).

### 타사 비교 — Notion이 출처

2026-07-27부터 타 증권사 앱과의 비교를 **Notion에 쌓는다.**
`Investment Monitor — 타사 비교 요구사항`
(https://app.notion.com/p/ef1de725813545709eba2498ab569c2d)

> **2026-07-29에 개인 워크스페이스로 옮겼다.** 처음 9건이 **회사 계정**에 만들어져
> 있었다 — 개인 프로젝트 기록이 있을 자리가 아니다. 워크스페이스를 넘나드는 이동은
> API로 안 돼서(`move-pages`·`duplicate-page` 모두 같은 워크스페이스 안에서만)
> 스키마를 다시 만들고 9건을 본문까지 옮겨 담았다. `적은 날`만 `생성 일시`(자동) →
> `날짜`(수동)로 바꿨다 — 안 그러면 옮긴 날짜로 전부 덮인다.
> `.claude/skills/broker-gap/SKILL.md`의 0절이 이 관문을 지킨다.

이 파일에는 **왜 그렇게 고쳤는지**를 남기고, 조사 결과와 진행 상태는 Notion에
둔다. 흐름은 `.claude/skills/broker-gap/SKILL.md`에 있다 — 조사(서브에이전트
병렬) → 비교 → Notion → 구현(서브에이전트) → 내 검수 → 커밋.

지금까지 쌓인 것:

| 기능 | 우리 앱 | 상태 |
|------|------|------|
| 매도 세율 2026년 인상(0.18% → 0.20%) | 있음(옛 값) | **완료** `d62dc6f` |
| **스톱지정가호가 — 거래소가 감시하는 스탑** | 일부만 | 시작 전 (높음) |
| 자동감시주문 — 종목별 스탑로스·트레일링 | 없음 | 시작 전 (높음) |
| 알림 체계 — 미체결·목표수익률·일등락률 | 없음 | 시작 전 |
| 조건 주문(가격 도달 시 자동 매매) | 일부만 | 시작 전 |
| 종목별 지정가 도달 알림 | 없음 | 시작 전 |
| NXT(넥스트레이드) — 08:00~20:00 거래 | 없음 | 시작 전 |
| 매매일지차트 — 차트에 매매 시점 표시 | 없음 | 시작 전 (낮음) |

**키움 조사에서 설계 질문 하나가 풀렸다.** "조건을 누가 감시하나" — 키움은
**서버가 감시하고 앱을 꺼도 돈다**(공식 도움말 0624). 우리 러너는 서버 메모리에
살고 재시작하면 멈춘다. 그건 의도된 결정이라("사람 모르게 되살아나 주문을 내는
쪽이 더 위험하다") **조건 주문은 그 결정과 정면으로 부딪힌다.** 만들려면 이
충돌부터 풀어야 한다.

우리 자동매매의 중단선(`stopEquity`)은 **계좌 전체 평가금액** 기준이라 종목별
손절과 성격이 다르다는 것도 확인했다(autoTrader.ts:105).

**2026-07-29 — 내가 쓴 판단이 틀렸던 것을 잡았다 (미래에셋·삼성 조사).**

두 증권사가 **독립적으로** `스톱지정가호가`를 말했다 — 스톱가에 닿으면 미리 정한
지정가로 주문이 나가는 **거래소 호가 유형**이다. 증권사 서버가 감시하는 자동감시주문과
다르다. 확인하러 KIS 공식 레포를 다시 훑으니 `docs/TRADING_API.md`에 내가 적어 둔
문장이 틀렸다:

> `CNDT_PRIC`… **이건 지속 감시가 아니다.** 주문을 접수시키는 시점의 조건가격일 뿐

접수된 뒤에도 조건이 남는다는 증거가 셋 나왔다 — `stpm_cndt_pric`(스톱지정가조건가격),
`stpm_efct_occr_yn`(**효력발생여부**), `stpm_efct_occr_dtmd`(**효력발생상세시각**).
실시간 WS에도 `ORD_COND_PRC`(*스톱지정가 시 표시*)가 있다. **접수 시점에 소비되는
값이라면 "효력발생여부"라는 필드가 존재할 수 없다.**

이게 왜 큰가 — 자동감시주문이 막혀 있던 이유가 *"우리가 직접 폴링해 감시해야 하고,
그건 「러너는 재시작하면 멈춘다」는 결정과 부딪힌다"*였다. **스톱지정가는 그 충돌이
없다.** 조건이 주문에 붙어 거래소로 가니 우리 서버가 켜져 있든 말든 상관없다.

**같은 날 주문 쪽도 풀렸다 — `ORD_DVSN = 22`다.** 처음엔 *"공식 샘플에 없으니 모른다"*로
남겼는데, **레포에 없었을 뿐 개발자센터 API 문서에는 다 있었다.** `CNDT_PRIC` 설명에
*"스탑지정가호가 주문 **(ORD_DVSN이 22)** 사용 시에만 필수"*라고 대놓고 적혀 있다.

**레포만 보고 "없다"고 쓴 대가로 세 곳이 틀려 있었다:**

| 적어 뒀던 것 | 실제 |
|------|------|
| `order_cash`의 `ord_dvsn`에 아무 목록도 없다 | 18개 코드가 **거래소별로** 다 있다 |
| `06`·`07`이 나오는 곳은 매수가능조회지 주문 전송이 아니다 | **KRX 주문 전송 목록에 있다** |
| `order_cash`는 `(KRX)` 하나만 적었다 | `KRX`/`NXT`/`SOR` 셋 다 받는다 |

**교차검증도 됐다** — 문서의 SOR 목록에 `21`~`24`가 없는데, 이건 미래에셋이
*"SOR 주문에는 중간가·스톱지정가호가를 제공하지 않는다"*고 적은 것과 정확히 일치한다.

**아직 안 해 본 것**: 실제로 `22`를 넣어 본 적은 없다. 모의투자는 `EXCG_ID_DVSN_CD`가
KRX만 가능하고, KRX는 `22`를 받지만 **모의 서버가 실제로 받는지는 별개 문제다.**
"문서에 있다"와 "넣어 봤다"를 섞지 않는다.

스킬(`.claude/skills/broker-gap/SKILL.md`)에 **"레포에 없다고 없다고 쓰지 마라"**를
문서 접근 경로와 함께 못 박았다 — 개발자센터는 SPA라 `WebFetch`로 안 열리고 브라우저로
`find` → `scroll_to`해야 읽힌다.

트레일링 스탑은 **여전히 자동감시주문 쪽에만 있다.** 고점을 따라 올라가는 건 누군가
계속 지켜봐야 하는 일이라 주문에 값 하나 달아서는 안 된다. 둘은 대체재가 아니다.

**출처 우선순위를 실측으로 고쳤다.** 처음엔 앱스토어 릴리스 노트를 1순위로
적었는데, 키움 조사 서브에이전트가 **그게 빈 값이라는 걸 잡아 왔다** — 영웅문S#은
최신부터 2024년까지 전부 `일부 기능 개선` 한 줄이다. 증권사 홈페이지 본문은
봇 차단되기도 했다(AhnLab).

실제로 통한 순서로 스킬을 고쳤다: **증권사 공식 도움말 사이트**(값까지 다 있다)
→ **앱스토어 스크린샷 원본**(실제 화면) → 차단 안 되는 안내 페이지 → 뉴스 →
블로그. **내가 쓴 지침도 틀릴 수 있다**는 사례로 스킬에 남겼다.

### 확인했지만 멀쩡했던 것

- **호가창은 이미 있고 잘 동작한다.** `종목 > 주문` 탭에서 국내 주식(예: 005930)을 고르면 주문 티켓 맨 위에 10단계 매도/매수 호가·잔량 막대·"09:06:58 기준 · 2초 전" 신선도 표시가 뜨고, 각 호가 행을 누르면 지정가 칸에 그 가격이 채워진다(`OrderBookPanel`, frontend/src/App.tsx:792-889). 동시호가 구간이면 예상 체결가, VI 발동 중이면 경고 문구도 따로 뜬다. 사전 브리핑의 "호가창이 없다"는 전제와 달리 실제로는 구현돼 있었다.
- 실계좌 매수가능 금액이 0원대로 부족할 때 `최대` 버튼은 `disabled={maxOrderQuantity === undefined || maxOrderQuantity <= 0}`로 정확히 잠기고(frontend/src/App.tsx:6489), 안내 문구도 "실계좌 예수금으로는 1주도 살 수 없습니다"처럼 0인 상황을 "최대 0주까지 살 수 있습니다"(살 수 있다는 말처럼 읽히는 문구)로 잘못 말하지 않는다. `CODE_STYLE.md`가 지적한 "0이나 빈 값일 때 문장이 뒤집히지 않는지" 기준을 실제로 지키고 있었다.
- 주문 확인 단계 이전에 실수로 `매수 주문하기`를 눌러도 `취소` 버튼으로 되돌릴 수 있고(frontend/src/App.tsx:6598-6606), `KIS_LIVE_ORDER_ENABLED`가 꺼진 지금은 최종 전송 버튼 자체가 잠겨 있어 실수로 실주문이 나갈 여지가 없었다(실측: `매수 주문하기` 버튼이 disabled 상태, 아래에 "실주문이 꺼져 있습니다…" 안내).
- KIS 계좌 조회가 도중에 502로 실패하는 것을 실측으로 봤는데(상단에 `KIS 계좌 조회 실패: 502` 배너), 그 아래 카드의 예수금·평가금액 숫자는 `-`나 0으로 지워지지 않고 마지막으로 받은 값(50,000원)을 그대로 유지했다. `CODE_STYLE.md`의 "받아 둔 값은 그대로 두고 오류만 올린다" 원칙이 실제로 지켜지고 있었다(다만 이 502가 이 점검 중 백엔드에 동시에 여러 API를 두드린 내 테스트 때문에 유발됐을 가능성이 있어, 자연 발생 빈도는 확인 못함 — `docs/TRADING_API.md`의 초당 호출 한도(EGW00201) 항목과 관련).
- 리스크 룰 카드의 `허용 종목` 칸에는 실제 값 "005930"이 굵은 흰 글씨로, `차단 종목` 칸에는 입력 예시가 옅은 회색 placeholder로 "005930"이 떠 있어 얼핏 같은 종목이 허용이면서 차단인 것처럼 보이지만, 실제로는 진하기 차이로 구분돼 있고 `GET /api/broker/kis/risk-rules`도 `symbolBlocklist: []`(빈 배열)를 반환한다 — 헷갈릴 뻔했지만 실제 버그는 아니었다.
- 예수금 50,000원, 삼성전자 현재가 25만원대에서 `실계좌 매수가능` 계산(`GET /api/broker/kis/orderability`)이 `cashBuyQuantity: 0`으로 정확히 나왔고, 주문 티켓의 경고 문구·`최대` 버튼 잠김과 API 응답이 서로 일치했다.

### 확인하지 못한 것

- **실제 주문 접수 → 체결/미체결 확인 → 정정·취소** 전체 흐름을 끝까지 밟지 못했다. 계좌(21) 실제 예수금이 50,000원뿐이라 삼성전자(허용 종목이 이것 하나뿐) 1주도 살 수 없어서(`실계좌 매수가능 49,751원, 최대 0주`), `KIS_LIVE_ORDER_ENABLED=true`로 게이트를 열어도 처음부터 리스크 룰/잔고에 막혀 실제 주문을 넣어볼 수 없었다. 이 작업 지침상 실주문 버튼도 누르면 안 됐으므로 어차피 시도하지 않았다.
- 미체결 주문의 `정정`(단가 입력) UI를 실제로 조작해보지 못했다 — 지금 계좌에 미체결 주문이 0건이라 정정 버튼 자체가 화면에 뜨지 않았다.
- 예약주문을 지금(정규장 중, 09:00~15:30) 등록 시도하면 어떤 메시지가 뜨는지 확인하지 못했다. `docs/TRADING_API.md`는 예약주문이 시간대·개장일 리스크 룰 검사를 건너뛴다고 적어뒀는데, 프런트의 `reservedOrderBlockers`(frontend/src/App.tsx:3550-3562)도 시간 검사를 하지 않아 지금 눌러도 버튼이 막히지 않을 것으로 보이지만, 실제로 KIS가 정규장 중 접수를 어떻게 처리하는지는 실주문이라 시도하지 않았다.
- 장 마감(15:30) 이후 화면이 실제로 어떻게 바뀌는지는 이번 점검 시간(장 시작 직후) 안에 관찰하지 못했다. 코드상 `장외 · 마감 후` 라벨 분기(frontend/src/App.tsx:1401-1412)는 확인했지만 실측은 못 했다.
- "선택 종목이 야간 환산가로 초기화되는" 현상(요구사항 5번)의 정확한 재현 조건 — 한 번 목격했지만 이후 여러 시도에서 재현되지 않았다.
- 계좌 21 외에 `KIS 23` 계좌는 전혀 들여다보지 않았다(잔고·리스크 룰 등 모두 계좌 21 기준).

## 2026-07-27 2차 — 주문 이후·계좌·발견

### 요약

- 1차에서 이미 고친 것(호가창, 수동 주문·예약주문의 리스크 룰 사전검사, 정정·취소의 게이트 사전검사, 첫 종목 선택, 수수료 어림값)은 실측으로 재확인만 하고 재기재하지 않았다. 전부 화면상 정상 동작했다.
- 이번에 가장 크게 걸린 것은 **매수/매도만 "주문 확인" 2단계를 거치고, 미체결 정정과 예약주문 등록은 버튼을 누르는 순간 바로 전송된다는 것**이다. 정정은 실제 가격을 바꾸는 조작인데 되돌아볼 확인창이 없다. 미체결 주문이 0건이라 화면에서 직접 눌러보지는 못했고 코드로 확인했다.
- **실측으로 새로 잡은 모순**: `발견` 탭 하단 카드는 지금(장중 11:2x)도 "정규장 이후"라고 적는데, 바로 옆 `종목` 탭 상단 배지는 같은 순간 "정규장 거래 중"이라고 정확히 적는다. 같은 실시간 상태를 두 곳이 다르게 말한다.
- **`종목 탐색`(사이드바 "탐색" 탭)이 새로고침할 때마다 "야간 환산가" 카테고리로 초기화되어, 주문 가능한 종목을 찾으려는 사람에게 후보 1개(그것도 주문 불가 종목)만 보여준다.** 카테고리를 손으로 바꾸면 국내 종목 300개가 나오는 진짜 탐색 도구가 있는데, 기본값이 그걸 가린다.
- 계좌(21) 예수금이 여전히 50,000원이라 1차와 마찬가지로 실제 매수 체결·정정·취소 전체 흐름은 끝까지 밟지 못했다(지침상 실주문도 금지). 미체결 주문·보유 종목이 하나도 없어 "이미 주문을 낸 뒤의 세계"의 상당 부분은 코드 판독으로 대체했다.

### 요구사항 (우선순위 순)

| # | 요구사항 | 왜 필요한가 (실제 시나리오) | 지금은 어떻게 되어 있나 | 근거(파일·API·화면) |
|---|---|---|---|---|
| 1 | 미체결 주문 `정정`(단가 변경)과 `예약주문 등록`에도 매수/매도처럼 "이렇게 바꿉니다 / 이렇게 넣습니다"를 보여주는 확인 단계를 넣어야 한다 | 매수/매도는 버튼을 누르면 `liveOrderConfirming` 상태로 바뀌어 종목·수량·가격·계좌를 다시 보여주고 "취소/주문 확인" 두 버튼 중 하나를 고르게 한다. 반면 미체결 주문의 `정정`은 새 단가를 입력칸에 쓰고 `확정`을 누르는 즉시 `submitAmendOrCancel`이 호출되어 그대로 전송되고, 예약주문도 폼을 채우고 `예약주문 등록`을 누르는 즉시 `submitReservedOrder`가 호출된다. 둘 다 요약 화면이나 되돌리기 단계가 없다. 지정가 자리에 0을 하나 더 붙이거나 자리를 잘못 눌러도 다시 확인할 기회 없이 실계좌로 나간다 — 정정은 실제 체결가를 바꾸는 조작이라 매수/매도보다 결코 가벼운 동작이 아니다 | `submitAmendOrCancel`(frontend/src/App.tsx:4123-4152)은 `정정` 버튼을 눌러 입력칸이 열린 뒤(App.tsx:7177-7212) `확정` 버튼(App.tsx:7188-7195)의 onClick이 직접 호출한다. `submitReservedOrder`(App.tsx:4065-4096)도 `예약주문 등록` 버튼(App.tsx:6673-6689)의 onClick이 직접 호출한다. 코드 전체에 `window.confirm`이 한 번도 쓰이지 않는다(grep 확인). 매수/매도만 `liveOrderConfirming` state(App.tsx:2079-2080)를 거쳐 요약 화면(App.tsx:7011-7050)을 보여준다. 계좌에 미체결 주문이 0건이라(`GET /api/broker/kis/open-orders` → `[]`) 실제로 `정정` 버튼을 눌러보지는 못했고, 이 항목은 코드 판독으로 확인했다 |
| 2 | `발견` 탭의 장 상태 카드(`정규장 이후`/`개장 전`/`주말 모드`)가 실제 KRX 정규장 시간(09:00~15:30)을 기준으로 계산되게 고쳐야 한다 | 2026-07-27(월) 11:27~11:33 KST, 즉 정규장이 한창 진행 중일 때 `발견` 탭 하단 "데이터 고지" 카드는 "정규장 이후 · (화) 08:45 · 다음 기준 시각까지 21시간 18분"이라고 적고 있었다. 같은 시각 `종목` 탭 상단 "장 상태" 배지는 정확히 "정규장 거래 중"이라고 적는다. 초심자가 `발견` 탭만 먼저 열어보면 "이미 장이 끝났나?"라고 오해하거나, 반대로 이 라벨을 믿고 장 마감 후에도 "아직 정규장 이후가 아니네"라고 잘못 판단할 수 있다 — 라벨이 실제 개장 여부와 무관하게 08:45를 기준으로만 갈리기 때문이다 | `getKoreanMarketCountdown`(frontend/src/App.tsx:1289-1317)은 `target.setHours(8, 45, 0, 0)` 딱 한 지점만 기준으로 삼아 `afterOpen`을 판정하고, 이 값이 `label: afterOpen ? '정규장 이후' : '개장 전'`(App.tsx:1313)로 그대로 화면(App.tsx:4776-4784)에 나간다. 08:45~다음날 08:45 사이 거의 24시간 내내(즉 실제 정규장 09:00~15:30을 포함한 전체 구간) "정규장 이후"로 표시된다. 반면 `종목` 탭이 쓰는 `getMarketSession`(App.tsx:1613-1636)은 KR 종목에 대해 실제 09:00~15:30을 기준으로 `정규장`/`장외`를 정확히 가른다(App.tsx:5586-5590 렌더). 실측: 11:27:06 기준 스크린샷 두 장에서 두 라벨이 동시에 다르게 떠 있는 것을 확인했다 |
| 3 | ~~해소(2026-07-27).~~ 기본값을 `kr-major`(국내 대표주, 실측 7개 후보)로 바꾸고 선택을 localStorage에 저장한다. 카테고리 목록은 서버에서 오므로(22개) `readStoredValue`의 허용 배열을 프론트에 박지 않고, **목록이 도착한 뒤에** 저장된 id가 아직 있는 값인지 걸러 없으면 기본값으로 되돌린다 — 서버가 카테고리를 지웠을 때 빈 목록으로 멈추지 않게. 원래 요구사항:  새로고침·최초 진입 시 기본 카테고리를 국내 상장 종목이 나오는 값(예: `국내 대표주`)으로 열어야 한다 | 페이지를 새로고침하고 `종목 > 탐색` 탭을 열면 카테고리가 항상 `야간 환산가`로 되돌아가 있고, 후보는 "1개 후보"("삼성전자 야간 환산가", 시세 칸은 `-`)뿐이다. 이 패널의 이름 자체가 `종목 탐색`(주문할 종목을 찾는 용도)인데 기본값이 주문 불가능한 종목 1개만 보여줘, 처음 쓰는 사람은 "탐색 기능이 비어 있다"고 오해하고 카테고리 드롭다운을 직접 눌러 봐야 진짜 기능(국내 전체 300개 후보 등)을 발견한다 | `activeCategory` 초기값이 `useState<string>('kr-night-proxies')`(frontend/src/App.tsx:1950)로 하드코딩돼 있고, 다른 UI 상태(`activePage`, `terminalTab`, `sidePanelTab`)와 달리 `readStoredValue`로 localStorage에 저장되지 않아 매 새로고침마다 이 값으로 되돌아간다. 패널 제목은 `SIDE_PANEL_TITLE.discover = '종목 탐색'`(App.tsx:335). 실측: 페이지를 새로고침한 직후 `탐색` 탭이 "1개 후보"·"야간 환산가"로 뜨는 것을 스크린샷으로 확인했고, 카테고리를 `국내 전체`로 바꾸면 즉시 "300개 후보"(두산·DL·유한양행 등 실제 KOSPI/KOSDAQ 종목)로 바뀌는 것도 확인했다(`fetchCategoryInstruments`, App.tsx:2644-2661) |
| 4 | ~~해소(2026-07-27).~~ `발견 > 랭킹`에 **거래소가 전 종목에서 매긴 등락률 순위 상위 30**을 붙였다(`GET /api/market/movers`, KIS `FHPST01700000`, 호출 1회). 기존 목록과 섞지 않고 `오늘 시장에서 많이 움직인 종목`(거래소) / `내 목록에서 많이 움직인 종목`(관심·최근) 두 블록으로 갈랐다. **상위 30만 온다는 것을 목록 위에 적는다** — 전 종목 목록이 아니다. 마스터에 없는 종목(30개 중 8개)은 눌리지 않고 `목록에 없음`이라고 적는다. 원래 요구사항:  "오늘 시장에서 많이 움직인 종목"을 찾아주는 기능인지, 아니면 "내가 이미 아는 종목 중 많이 움직인 것"만 보여주는 기능인지 화면에서 더 분명히 하거나(이미 부제로 밝히고 있어 정직하긴 하다), 전체 종목을 등락률로 정렬해 보여주는 경로를 하나는 마련해야 한다 | `랭킹` 탭 부제는 "최근·관심 종목 중에서 · 전일 종가 대비"라고 정직하게 적혀 있어 속이지는 않는다. 하지만 관심종목·최근 본 종목·발견용 고정 12종목·탐색에서 검색한 종목을 합친 것 안에서만 순위를 매기므로(App.tsx:3219-3244), 지금처럼 관심종목이 거의 비어 있으면 "많이 움직인 낯선 종목을 찾는다"는 목적을 이 탭이 채워주지 못한다. 그렇다고 `탐색` 탭에서 국내 전체 300개를 등락률 순으로 정렬해 볼 수 있는 것도 아니다(정렬 컨트롤 없음, 코드순으로만 나열됨). 결과적으로 앱 전체에 "오늘 시장에서 가장 많이 오른/내린 종목"을 실제로 찾아주는 화면이 하나도 없다 | `moversBoard`(App.tsx:3219-3244)는 `[...terminalItems, ...recentInstruments, ...watchlist, ...categoryItems]`만 후보로 삼아 `Math.abs(changeRate)`로 정렬한다. 랭킹 탭 부제(App.tsx:5136-5138)는 "최근·관심 종목 중에서"라고 이미 밝히고 있다. `탐색` 탭(카테고리 `국내 전체`, 실측 300개 후보)에는 정렬 드롭다운이나 버튼이 없고, 목록 순서가 두산(000150)·DL(000210)·유한양행(000100)·... 순으로 종목코드 오름차순으로 보였다 |
| 5 | ~~해소(2026-07-27).~~ `기간별 매매손익`에 `오늘` 구간을 넣고 기본값으로 삼았다(실측 부제 `26.07.27 ~ 26.07.27`). 서버 하한이 1이라 아무리 좁혀도 어제부터였던 것도 함께 고쳤다 — `오늘`이라고 적으면서 어제 값을 섞을 뻔했다. 평가손익(누적 미실현)과 실현손익은 **합치지 않았다**. 원래 요구사항:  실현손익만 볼 수 있는 기간 선택지를 추가하거나, 평가손익(누적 미실현)과 오늘의 실현손익을 합친 "오늘 총 손익" 한 줄을 어딘가에 둬야 한다 | "오늘 얼마 벌었나"를 확인하려면 두 카드를 봐야 한다 — `KIS 실계좌` 카드의 `평가 손익`(전체 보유분의 누적 미실현 손익, 오늘 하루의 변화량이 아니다)과 `기간별 매매손익` 카드의 `실현손익`(선택한 기간 전체의 확정 손익)이다. 그런데 `기간별 매매손익`은 `1개월`·`3개월`·`1년`만 고를 수 있어 "오늘 판 것만"을 따로 볼 수 없다 — 기본값(1개월)을 선택하면 오늘 실현한 손익이 지난 한 달치와 합산된 숫자로만 보인다. 두 손익 항목이 라벨상 헷갈리지는 않지만(평가손익/실현손익으로 잘 구분돼 있다), "오늘 하루"라는 시간축을 어느 카드도 제공하지 않는다 | `TRADE_PROFIT_RANGES`(frontend/src/App.tsx:1490-1494)는 `{days:30,'1개월'} / {90,'3개월'} / {365,'1년'}` 세 개뿐이고 기본값은 `tradeProfitDays = 30`(App.tsx:2067). "오늘"(당일)에 해당하는 선택지가 없다. `평가 손익`은 `kisAccountSnapshot.unrealizedPnl`(App.tsx:5987-5990)로 계좌 전체 보유분 기준이라 "오늘 얼마 변했는지"가 아니다. 계좌(21)는 현재 보유 종목·확정 손익이 모두 0이라 값 자체는 실측하지 못했고, 선택지 구성과 기본값은 코드로 확인했다 |
| 6 | ~~해소(2026-07-27).~~ **다만 원래 지적의 전제가 일부 틀렸다** — 실측하니 `주문 잠김` 배지와 `내 계좌` 탭이 첫 화면에 이미 있었다(`주문`·`계좌` 둘 다 화면에 존재). 없던 것은 **`실계좌`**였다: `주문 잠김`은 "어떤 주문 기능이 꺼져 있다"로 읽히지 열리면 실제 돈이 나간다는 말이 아니다. 배지를 `실계좌 주문 잠김`/`실계좌 주문 가능 · 실전 서버`로 바꾸고(모든 화면 상단), 첫 화면 설명 아래에 한 줄을 넣었다 — "이 앱은 한국투자증권 실계좌로 국내 주식을 사고팔 수 있는 도구입니다 — 주문은 종목 화면에서 내고, 잔고·손익은 내 계좌에서 봅니다. 지금은 실주문이 잠겨 있어 주문이 나가지 않습니다." 배너로 키우지 않고 게이트 상태를 그대로 적는다. 원래 요구사항:  이 앱이 "실계좌로 매매 주문을 낼 수 있는 도구"라는 것을 한 줄이라도 밝히거나, 첫 화면에서 주문 화면으로 가는 안내를 둬야 한다 | localStorage가 없는 완전히 새 사용자는 상단 네비 `종목`이 아니라 `발견` 탭(기본값)의 `대시보드` 하위탭(기본값)으로 진입한다. 이 화면 제목은 `발견`, 부제는 "야간 지표 · 시세 조회", 설명은 "야간 지표를 띄워 두고, 아래 탭에서 시세 흐름·뉴스·커뮤니티·도구를 살펴봅니다"이며, 화면 어디에도 "매매"·"주문"·"계좌"라는 말이 없다. 앱 헤더 자체도 이름표(eyebrow) "실시간 차트 · 뉴스 · 출처 터미널", 제목 "KidChang-Charts"로, 문구만 보면 시세·뉴스를 보여주는 조회 전용 도구처럼 읽힌다. 실제로는 실계좌로 국내 주식을 매매할 수 있는 도구인데, 이 사실이 첫 화면 어디에도 적혀 있지 않아 처음 쓰는 사람은 "다음에 뭘 눌러야 하는지" 스스로 상단 탭 세 개를 눌러보며 알아내야 한다 | `activePage` 기본값은 `readStoredValue('activePage', 'terminal', ...)`(frontend/src/App.tsx:2008-2010)로, localStorage가 비어 있으면 `'terminal'`(발견)이다. `terminalTab` 기본값도 `'overview'`(App.tsx:2012-2014, 대시보드). 헤더 문구는 App.tsx:4228-4229(`실시간 차트 · 뉴스 · 출처 터미널` / `KidChang-Charts`), 발견 페이지 설명은 App.tsx:4616-4624. 실측: 이 세션의 localStorage를 지우지 않고도(지침상 금지) 코드의 기본값 로직과 실제로 새로고침 시 이 화면들이 뜨는 것(대시보드·헤더 문구는 사용자별 데이터에 의존하지 않는 정적 문구라 실측과 코드가 일치함)을 확인했다 |

### 확인했지만 멀쩡했던 것

- **수동 주문 티켓과 예약주문 카드 모두 이제 게이트·리스크 룰을 사전 검사해 버튼을 잠그고 사유를 나열한다.** 실측: SK하이닉스(000660, 허용 종목 아님)를 선택한 채로 주문 티켓을 열자 "허용 종목 목록에 없습니다(000660). 허용: 005930", "1회 주문 금액 한도 1,000,000원을 넘습니다", "실계좌 매수가능 금액을 초과합니다(49,751원)", "실계좌 예수금으로는 1주도 살 수 없습니다" 네 사유가 `매수 주문하기` 버튼 위에 바로 떴고 버튼은 비활성 상태였다. 예약주문 카드도 같은 종목으로 "허용 종목 목록에 없습니다(000660)... · 지정가를 입력하세요. · 실주문이 꺼져 있습니다..."를 똑같이 미리 보여줬다.
- **정정·취소는 게이트만 보고 리스크 룰은 의도적으로 보지 않는다**는 설계(App.tsx:3717-3739 주석)가 실제 코드와 일치했다. 취소가 리스크 룰(허용 종목 등)에 막혀 영영 못 거두는 상황을 피하려는 의도적 결정이라고 주석에 명시돼 있다.
- **자동매매 패널은 화면만 보고도 무엇을 하는 물건인지 알 수 있었다.** 부제 "규칙대로 사고팔다가 목표나 중단선에 닿으면 스스로 멈춥니다 · 수익을 보장하지 않습니다"가 과장 없이 적혀 있고, 전략 드롭다운 아래 실제 백테스트 검증 문구("2026-07 측정(20종목·전체 기간을 3구간으로): 구간별 중앙값 +19.21%/+12.27%/-4.86%. ... 지금이 어느 쪽인지 알 수 없다")가 붙어 위험을 숨기지 않았다. 실행 방식을 `연습`/`실제`로 명확히 구분하고 버튼 문구도 `연습 시작`/`실제 매매 시작`으로 달라져(App.tsx:6327) 지금 무엇을 시작하려는지 헷갈리지 않았다. 시작 버튼은 지침에 따라 누르지 않았다.
- **예약주문 카드가 "언제 나가는지"를 화면에서 반복해 말한다.** 카드 부제("취소하지 않으면 다음 개장일에 주문이 나갑니다")와 빈 목록 문구("예약주문이 없습니다 · 아래에서 등록하면 다음 개장일에 주문이 나갑니다")가 같은 내용을 카드 상단·본문 두 곳에서 일관되게 알려줬다.
- **종목 탐색 카테고리를 직접 바꿔 보면 실제로 쓸모 있는 종목 브라우징 도구가 있었다.** `국내 대표주`·`국내 전체`(300개)·`KOSPI`·`KOSDAQ`·각종 ETF 세분류(`KODEX`·`TIGER`·`ACE`·`KIWOOM`·배당/커버드콜·채권/머니마켓·레버리지/인버스)·`미국 대표주`·`미국 전체`까지 카테고리가 21개 있었다. 기본값 문제(요구사항 3번)만 아니면 발견 목적에 충분히 쓸 만한 도구였다.

### 확인하지 못한 것

- **미체결 주문의 `정정` 입력칸을 실제로 열어보지 못했다.** 계좌(21) 예수금이 50,000원뿐이고 허용 종목이 005930 하나뿐이라 지정가 매수 자체를 넣을 수 없었고(지침상 실주문 버튼도 금지), 미체결 주문이 0건이라 `정정`·`취소` 버튼이 화면에 아예 나타나지 않았다. 요구사항 1번은 `submitAmendOrCancel`·`submitReservedOrder` 코드를 읽어 확인한 것이지 화면에서 직접 눌러본 것이 아니다.
- **예약주문을 지금(정규장 중, 09:00~15:30)이나 15:40~07:30 등록 허용 시간대에 실제로 등록해 KIS가 어떻게 응답하는지는 이번에도 확인하지 못했다.** 1차와 같은 이유(게이트가 닫혀 있고 실주문 지침상 시도 금지)다. 다만 실계좌 주문 기록에서 2026-07-26 00:04:52(등록 허용 시간대 안)에 시도된 과거 예약주문 등록이 "서비스 점검중입니다"로 거부된 기록을 봤는데, 이건 KIS 쪽 점검 시간과 겹친 것으로 보여 "정규장 중 등록 시도 시 응답"이라는 원래 질문에는 답이 되지 않는다.
- ~~**`발견` 탭의 "정규장 이후" 오표시(요구사항 2번)가 실제 장 마감(15:30) 이후에는 문구가 바뀌는지**~~ — **2026-07-27 15:34:02 실측으로 확인했다. 정상이다.** `5eefb55`에서 두 화면이 `KRX_SESSION` 하나를 함께 쓰도록 고친 뒤라, 마감 직후 `발견`은 `정규장 마감` · `다음 개장 내일 09:00` · "오늘 정규장은 끝났습니다"로, `종목`은 `장외 / 마감 후`(tone=`closed`)로 **함께** 넘어갔다. 15:26에는 둘 다 정규장이었다.
- 계좌 21 외에 `KIS 23` 계좌는 이번에도 들여다보지 않았다.
- 관심종목을 실제로 여러 개 채운 상태에서 `발견 > 랭킹`이 어떻게 달라지는지(요구사항 4번이 그 반대 상황, 즉 관심종목이 거의 빈 상태만 확인했다)는 시도하지 않았다 — 지침의 "localStorage를 비우지 마세요"와 별개로 관심종목을 대량으로 채우는 것 자체가 이번 점검 범위를 벗어난다고 판단했다.

---

## 2026-07-31 밤 — 종목 폭을 3개에서 4,035개로 (커밋 7건)

### 무엇이 바뀌었나

| | 전 | 후 |
|------|------|------|
| 볼 수 있는 종목 | 감시목록 **3개**(`watchlist.ts` 하드코딩) | **4,035개** + 테마 302개 |
| 분야 분류 | 없음 | 지수업종 2,498건 + **테마 302개** |
| 시세 조회 | 종목당 1회 | **30종목당 1회** |
| 스크리닝 | 40종목 40회 6초 | **120종목 4회 0.48초** |
| 마스터 파일 | 손으로 넣음(20일 묵음) | **코드로 받음** |

```
ceaf248  다섯 전문 에이전트 + trading-loop 스킬
48fb29a  종목명 off-by-one — 8종목 복구
51cae71  지수업종 대·중분류 2,498/1,709건
b3c390f  KIS 테마 302개 · 5,528쌍
d4c455f  멀티시세 30:1
564e85c  테마별 등락률 (라우트 3개)
4ac0a5c  공개 마스터 다운로드 자동화
```

시험 98 → **219건**. 매 단계를 **구현자와 다른 에이전트**로 교차 검증했다.

### ★ 장중에 다시 재야 할 것 — 전부 마감 후에 쟀다

이번 회차의 모든 측정이 **2026-07-30 종가 고정** 상태에서 나왔다
(`sessionElapsedRatio() = 1`). 다음 개장일에 이것부터 잰다.

1. **테마 등락률이 장중에 움직이나.** `GET /api/themes/pulse?codes=017`을 09:05·11:00·
   14:00에 재서 값이 바뀌는지. 마감 후엔 세 번 불러도 같은 값이 나온다
2. **동시호가 구간(08:30~09:00, 15:20~15:30)에 멀티시세가 무엇을 주나.**
   응답에 `intr_antc_*`(예상체결) 필드가 있는데 **한 번도 값이 든 것을 못 봤다.**
   단건(`FHKST01010200`)은 예상체결을 주는데 멀티는 어떤지 모른다
3. **부분비례 유동성 문턱.** `screenQuote`가 `MIN_DAILY_TURNOVER × elapsed`로 거른다.
   `elapsed < 1`인 상태를 한 번도 안 태웠다 — 09:05에 잰 값과 15:00에 잰 값이
   같은 잣대가 아니라는 것이 화면에 제대로 드러나는지
4. **거래대금 어림 오차가 장중엔 얼마나 벌어지나.** 오차 = `(VWAP − 종가)/종가`인데
   장중에는 종가가 계속 바뀐다. 마감 후 실측은 중앙값 +0.28% / 범위 −6.68%~+6.24%였다
5. **멀티시세와 단건이 같은 틱을 주나.** 마감 후에는 9필드가 전부 일치했지만,
   값이 움직이는 중에도 그런지는 모른다
6. **스크리닝 판정이 장중에 뒤집히나.** 마감 후 113종목에서 뒤집힘 0건이었는데,
   `000105 유한양행우`가 문턱 대비 −1.9%로 아슬아슬했다

### 확인하지 못한 것 (화면)

**브라우저 확장이 안 붙어 이번 회차의 화면 검증은 전부 열려 있다.** 재무 표가
높이 0으로 접혀 있던 전례가 있어 이 항목을 닫지 않았다.

- 테마 탭이 실제로 어떻게 보이는지 (기준일·표본 사슬·빈 테마 3개)
- `skipped`(예산 초과) 문구가 화면에 뜨는지
- 하드코딩 테마 보드를 지운 뒤 레이아웃이 깨지지 않았는지
- 프론트 청크 30이 실제로 요청 4회를 내는지 (코드·산술로만 확인)

### ~~아직 판단이 필요한 것~~ (2026-07-31 장중에 갈랐다 → 아래 절)

**거래가 한 건도 없는 종목의 `0%`가 테마 등락률 중앙값·보합 수에 들어간다.**
가중평균에서는 `turnover > 0`으로 정확히 뺐는데 나머지에는 남아 있었다.

| 테마 | 그때 | 거래 0원 제외 | 차이 |
|------|------|------|------|
| 2차전지(소재,부품,장비) | +0.810 | +1.090 | **+0.280%p** |
| 반도체/반도체장비 | −2.580 | −2.815 | −0.235%p |

2차전지는 **보합 4종목이 전부 거래 0원**이라 진짜 보합은 0종목이었다. 장중에
재 보니 「거래대금 0원」 안에 성질이 다른 둘이 섞여 있었다 — 아래 절에서 갈랐다.

### 남은 결함 (이번 범위 밖이라 안 고침)

- **`ffcode.mst`(해외선물) 674/4,920행의 이름이 잘려 있다.** 이름 필드가 `[82,132)`
  50바이트인데 `slice(82,107)`로 자른다. 증상이 국내보다 나쁘다 — **만기가 잘려
  서로 다른 계약이 같은 이름**이 된다(`IHBU26`·`IHBZ26`·`IHBH27` → 전부
  `iShares $ Investment Grad`). 같은 파일의 `isSpread`는 `'Y'`를 절대 못 읽는 죽은 코드다
- **`0007N0 아이엠에셋 200`이 마스터는 `EF`인데 DB는 `stock`이다.** 단축코드에 영문자가
  섞여 `inferDomesticAssetType`이 못 잡은 것으로 보인다(추정)
- **KOSDAQ 264종목에 업종이 없다.** 우리 파싱 문제가 아니라 **KIS가 `1032 통신`·
  `1033 IT 서비스`를 한 건도 안 채운다**(idxcode에는 있다). 한글과컴퓨터·안랩·컴투스·
  KG이니시스·카페24가 그렇다. 대조군: KOSPI는 같은 업종을 갖고 있다
- **테마 명단이 2025-11-06에 멈춰 있다.** 종목 마스터를 새로 받을수록 **매칭이 나빠진다** —
  218종목/425쌍 → 220종목/432쌍(20일치 갱신 후). 구멍 난 테마 192 → 197개

### 이번에 지킨 선

주문·정정·취소·예약주문 **전송 0건**(`trading_broker_orders`에 `submitted` 여전히 0).
자동매매 `stopped`, 리스크 룰·`.env` 무변경. **실주문 게이트는 사용자가 열어 둔 채로
두었다** — 여는 것도 닫는 것도 사용자 몫이다.

---

## 2026-07-31 장중 — 비용 문턱이 시간비례여야 하나 (과거 3개월을 재구성해 쟀다)

### 무엇이 문제였나

`screenQuote`의 두 문 중 **유동성만 `elapsed`를 곱하고 비용은 안 곱한다.**

```ts
const turnover = quote.turnover ?? quote.price * quote.accVolume;
if (turnover < MIN_DAILY_TURNOVER * elapsed) return 'illiquid';   // ← elapsed 곱함

const rangeRate = knownRangeRate(quote);
if (rangeRate !== undefined && ROUND_TRIP_COST_RATE > rangeRate * MAX_COST_SHARE_OF_RANGE) {
  return 'costHeavy';                                             // ← elapsed 안 곱함
}
```

`ROUND_TRIP_COST_RATE = 0.43%`, `MAX_COST_SHARE_OF_RANGE = 0.5` →
**필요한 변동폭 ≥ 0.860%**. 이 잣대는 하루치인데 장 초반에 그대로 걸린다.
장중 실측(스크리닝 풀 120종목)에서 09:04에 `costHeavy`가 18종목이었다.

거래대금은 시간에 선형으로 쌓이지만 고가−저가는 **누적이 아니라 범위**라
같은 방식으로 나눌 수 없다. 그럼 어떻게 나눠야 하는지를 **재서** 정했다.

### 잰 방법 — 과거를 재구성할 수 있다

주식일별분봉조회(`FHKST03010230`)는 `FID_INPUT_DATE_1`로 **과거 날짜**를 받고
`FID_INPUT_HOUR_1`로 끝 시각을 정해 거기서 뒤로 120봉을 준다. 2025-10-31까지
거슬러 받아지는 것을 확인했다(`scripts/probeIntradayHistory.ts`).

| HOUR_1 | 받은 것 |
|------|------|
| `235959` / `153000` | 13:21~15:30 (120봉) |
| `130000` | 11:01~13:00 (120봉) |
| `110000` | 09:01~11:00 (120봉) |
| `093000` | 09:00~09:30 (31봉, 장 시작에서 잘린다) |

정규장 09:00~15:30은 391분이라 **창 다섯 개**를 이어 붙여야 하루가 덮인다 —
종목·하루당 KIS 5회다. `getInstrumentIntradayCandles`는 오늘 날짜가 박혀 있고
120봉만 주므로 이 경로로는 못 한다.

이어 붙인 것이 맞는지는 **일봉의 고가·저가와 정확히 대조**했다. 한 창이라도
빠지면 하루 고저가 달라지므로 봉 수를 세는 것보다 엄한 검사다. 안 맞으면 버렸다.

- 도구: `scripts/measureRangeExpansion.ts`(받아서 재구성) →
  `scripts/analyzeRangeExpansion.ts`(받아 둔 것만 읽어 해석). 계산부는
  `trading/rangeExpansion.ts`에 순수 함수로 두고 시험 22건으로 덮었다.
- 표본: **50종목 × 15거래일**(2026-04-28 ~ 07-30, 3개월에 고르게 흩뿌림).
  시도한 750건 중 **632건 사용**, 110건(14.7%)은 고저 불일치로 버림, 8건은 봉 없음.
  시각 12개 × 632 = 7,333표본. 그중 유동성 문 통과 5,720건(78.0%).
  KIS 3,800회, 약 22분 걸렸다.
- 두 값을 잰다. `rangeRate`(개장~t 누적 폭 ÷ 그 시각 값) = **지금 잣대가 보는 것**,
  `aheadRangeRate`(t 이후~마감 폭 ÷ 그 시각 값) = **t에 들어간 사람 앞에 남은 폭**.
  둘 다 **그 시각의 값**으로 나눈다 — 종가로 나누면 미래를 훔쳐 쓴다.

### 재구성이 실측을 재현한다 (도구 검산)

같은 잣대를 같은 방식으로 센 값이다. 실측은 2026-07-31 하루, 재구성은 15거래일 중앙값.

| 시각 | 2026-07-31 실측(120종목) | 재구성 중앙값(632종목·날) |
|------|------|------|
| 09:04 / 09:05 | `costHeavy` 18 = **15.0%** · 누적폭 **1.729%** | **15.2%** · **1.61%** |
| 09:35 / 09:30 | 14 = **11.7%** · **2.707%** | **9.2%** · **2.41%** |
| 10:00 | 12 = **10.0%** · **3.088%** | **8.5%** · **2.60%** |

2026-07-31 11:21에 `runScreening`을 그대로 한 번 더 돌려 재확인했다(120종목,
`elapsed` 36.2%): `pass` 90 · `illiquid` 21(17.5%) · `costHeavy` 9(7.5%) ·
누적폭 중앙값 3.392%. 재구성 11:00은 22.9% / 6.5% / 3.02%다.

### ★ 결과 1 — `elapsed`도 √t도 **기각**된다

폭이 벌어지는 속도가 둘 다보다 훨씬 느리다.

| 구간 | elapsed | √elapsed | **실제 폭(중앙값 비)** |
|------|------|------|------|
| 09:01 → 09:05 | 5.00배 | 2.24배 | **1.68배** |
| 09:05 → 09:15 | 3.00배 | 1.73배 | **1.27배** |
| 09:15 → 09:30 | 2.00배 | 1.41배 | **1.16배** |
| 09:30 → 10:00 | 2.00배 | 1.41배 | **1.11배** |
| 11:00 → 12:00 | 1.50배 | 1.22배 | **1.08배** |

**09:01에 이미 하루치 폭의 25.2%가 벌어져 있다.** 09:05 43.2% · 09:30 70.0% ·
11:00 90.3%다. `elapsed`(09:01에 0.26%)를 곱하면 문턱이 사실상 사라진다.

### ★ 결과 2 — 지금 문턱은 초반에 빡빡하고 후반에 헐겁다 (오차의 부호가 뒤집힌다)

"t에 들어갈 값어치가 있었나"를 `aheadRangeRate ≥ 0.860%`로 놓고 견줬다.
유동성 문 통과분 5,720건 기준.

| 시각 | 앞이 넉넉한 비율 | 탈락률 | **헛탈락**(넉넉한데 버림) | **놓침**(모자란데 통과) |
|------|------|------|------|------|
| 09:01 | 95.6% | 38.9% | **34.7%p** | 0.2%p |
| 09:05 | 92.9% | 21.6% | **15.6%p** | 1.1%p |
| 09:30 | 90.5% | 11.6% | 4.6%p | 2.5%p |
| 11:00 | 85.0% | 8.5% | 1.2%p | 7.8%p |
| 13:00 | 77.4% | 7.7% | 0.2%p | **15.1%p** |
| 15:00 | 42.8% | 7.2% | 0.2%p | **50.2%p** |

`rangeRate`가 쓸모없다는 뜻은 아니다 — `aheadRangeRate`와의 **순위 상관이
0.54~0.72**로 뚜렷하다. 틀린 것은 신호가 아니라 **잣대의 높이**다.
그리고 `elapsed`·√t는 후반의 놓침을 하나도 못 고친다(그때 `elapsed`≈1이라 셋이 같은 값이다).

### ★ 결정 — 상수는 바꾸지 않는다. 대신 구멍 하나를 고쳤다

바꾸지 않는 이유:

1. 시간비례·√t는 **실측으로 기각**됐다. 남은 둘("그냥 둔다" / "초반 유예") 중
   유예는 "언제까지가 초반인가"라는 새 상수를 3개월·한 풀에서 fitting하는 일이다.
2. **날마다 크게 흔들린다.** 09:01의 탈락률이 15거래일에 걸쳐 24.0%~51.7%
   (중앙값 36.7%), 10:00은 5.4%~25.0%다. 이 폭 안에서 상수를 정할 수 없다.
3. 제대로 고치려면 "지나간 폭" 대신 **"앞으로 남을 폭"**을 재야 한다. 그건 상수
   조정이 아니라 문턱이 무엇을 묻는지를 바꾸는 일이고, 그 추정기 자체를 다시
   재야 한다.

고친 것 — **`if (range > 0)` 가드가 만든 구멍**(`knownRangeRate`):

예전에는 이 가드 하나로 "고가·저가를 모름"과 "한 값에만 체결됨"을 함께
건너뛰었다. 그래서 **1원 움직인 종목은 `costHeavy`로 버리면서 한 푼도 안 움직인
종목은 통과**시켰다. 문턱을 어디에 두든 이 순서는 성립하지 않는다 — 폭이
넓을수록 판정이 나빠질 수는 없다. 실측: `000227 유유제약2우B`가 09:35·10:00
두 회차 모두 `pass`였고, 그 사이 거래대금이 8,684,600원으로 **1원도 안 바뀌었다.**

이제 고가·저가를 못 받았으면 `undefined`(안 거른다), 받았는데 한 값이면 `0`
(`costHeavy`)이다. `runScreening`도 같은 함수를 쓴다 — 예전에는 화면에는 `없음`,
판정은 `pass`로 두 자리가 다른 규칙을 들고 있었다.

**이 변경이 얼마나 바꾸는지도 쟀다** (전체 표본 대비 `costHeavy` 비율):

| 시각 | 고침 전 | 고침 후 | 차이 |
|------|------|------|------|
| 09:01 | 23.0% | 33.1% | **+10.1%p** |
| 09:05 | 15.2% | 17.7% | +2.5%p |
| 09:15 | 11.7% | 12.5% | +0.8%p |
| 09:30 이후 | — | — | **+0.0~0.2%p** |

**대가를 숨기지 않는다**: 09:01의 고가=저가 종목(유동성 통과분 54건) 중 85.2%는
앞으로 0.860% 넘게 벌어졌다 — 그 자리는 이 변경이 잘못 버리는 쪽이다. 다만
09:30 이후로는 고가=저가면서 유동성을 통과한 표본이 500건에 0~1건이고, 그중
앞이 넉넉했던 것은 **0%**였다.

### 더 재야 정할 수 있는 것

- **보유 기간이 정해져 있지 않다.** `aheadRangeRate`는 **같은 날 안에 되판다**는
  가정이다. 그런데 `autoTrader.loadCandles`는 **분봉**을 전략에 넣고
  (`getInstrumentIntradayCandles`), 성과 측정(`scripts/measureStrategies.ts`)은
  **일봉**으로 돌린다. 하루치 잣대가 맞는지 며칠치 잣대가 맞는지는 이걸 먼저
  정해야 답이 나온다. **이 문턱을 손대기 전에 이것부터 정하는 게 순서다.**
- **한 장세뿐이다.** 3개월(2026-04~07)에 흩뿌렸지만 그 안에서 국면이 갈리는지는
  안 봤다. 급락일·변동성 확대 국면을 따로 갈라 재야 한다.
- **풀이 좁다.** `kr-all`+`kr-etf` 앞 50종목이라 우선주와 소형 ETF가 많다.
  도구는 종목 수 인자를 받으므로 300종목으로 다시 돌릴 수 있다(시간만 든다 —
  50종목 × 15일이 22분이었으니 300종목이면 2시간이 넘는다).
- **ETF 재구성이 자주 어긋난다.** 시도한 750건 중 110건(14.7%)이 일봉 고저와 안
  맞아 버려졌고 대부분 ETF다(`0000D0`은 15일 중 10일). **원인은 확인하지
  못했다**(수정주가·LP 호가·분봉에 안 잡히는 체결 중 무엇인지 모른다). 지금은
  버리고 있어 결과가 틀리지는 않지만, **ETF가 표본에서 과소 대표된다.**

### 이번에 지킨 선

주문·정정·취소·예약주문 **전송 0건**. 자동매매 `stopped` 그대로, 리스크 룰·
`.env`·`.cache/` 무변경. 실주문 게이트는 **손대지 않았다**(사용자가 열어 둔 상태).
KIS는 시세·분봉 조회만 했다.

---

## 2026-07-31 장중 — 거래정지 종목이 「거래대금 부족」으로 걸리던 것

정규장(11:33~11:35 KST, 실전 서버) 실측. **읽기만 했다.**

### 재기 전 물음

거래정지 종목을 **호출을 늘리지 않고** 알아볼 수 있나. 그리고 **개장 직후 정상
종목**과 구별되나 — 09:00대에는 정상 종목도 거래량 0·시고저 0·거래대금 0이다.

### ★ 두 API가 정확히 반대로 갈라져 있다

| | 멀티시세 `FHKST11300006` (29필드) | 단건 현재가 `FHKST01010100` (80필드) |
|------|------|------|
| 총 매도·매수 잔량 (`total_askp_rsqn`·`total_bidp_rsqn`) | **있다** | **없다** — 이름에 `rsqn`·`askp`·`bidp`가 든 필드 0개 |
| 종목 상태 `iscd_stat_cls_code` (거래정지 = `58`) | 없다 | **있다** |

거래정지 플래그가 가장 정확하지만 단건에만 있어 **종목당 1회**가 더 나간다
(120종목 스크리닝이 4회 → 124회). 잔량은 이미 오는 값이라 그쪽을 쓴다.

### 300종목에서 잰 것 (`kr-all` + `kr-etf` 앞 300)

| 무엇 | 몇 종목 | 되물은 `iscd_stat_cls_code` |
|------|------|------|
| 총잔량이 **양쪽 다 0** | **6** — 000300 DH오토넥스 · 000880 한화 · 00088K 한화3우B · 001470 삼부토건 · 001570 금양 · 001840 이화공영 | **6종목 전부 `58`(거래정지)** |
| 한쪽만 0 | 1 — `002070 비비안` **+29.83% 상한가 잠김** (총매도 0 · 총매수 25,424 · 거래대금 14.7억) | 정상 |
| 거래대금 0원인데 호가는 있음 | 2 — `0000Y0 HK 26-12 회사채 액티브`(3,000/6,000) · `001067 JW중외제약2우B`(36/35) | 정상 (`57`) |
| 호가가 있는 종목 표본 15 | — | `58` **0건** |

테마 등락률 경로에서 추가로 잡힌 4종목(`009440 KC그린홀딩스` · `011810 STX` ·
`002410 범양건영` · `044180 KD`)도 되물어 전부 `58`이었다.
**누적 12종목 전부 일치, 반대 방향 표본 15종목에서 놓친 것 0건.**

썼다가 버린 후보 둘:
- `temp_stop_yn`(임시정지여부) — 거래정지 12종목이 **전부 `N`**이다. 다른 뜻의 필드다
- `mang_issu_cls_code`(관리종목) — 12종목 중 9종목이 `Y`지만 거래정지와 다른 축이다
  (`000300`·`000880`·`00088K`는 관리종목이 아닌 채로 거래정지다)

### ★ 개장 직후 정상 종목과는 갈린다 — 시세 값으로는 못 가른다

`0000Y0`이 11:33에 **거래대금 0원 · 거래량 0 · 시고저 0**이면서 호가는
3,000/6,000이었고 종목 상태는 정상이었다. 시세 필드만 보면 거래정지와 똑같다.
09:00대의 정상 종목이 정확히 이 모양이다 — **갈라 주는 것은 호가뿐이다.**

다만 **장전 주문 접수(08:30) 전과 휴장일은 재지 않았다.** 그때는 정상 종목도
호가가 비어 있을 것이라, 판정 이름을 `거래정지`가 아니라 **`호가 없음`**으로 뒀다.

### 고친 것을 장중에 다시 태워 본 결과

`runScreening(현금 100만원, 300종목)` · 장 경과 43.4% · KIS 10회:

| 판정 | 종목 |
|------|------|
| `pass` | 222 |
| `illiquid` 거래대금 부족 | 51 |
| `costHeavy` | 19 |
| **`noOrderBook` 호가 없음** | **6** (위 6종목과 정확히 일치) |
| `tooExpensive` | 2 |

현금을 5만원으로 낮춰도 `000880`(83,800원)·`00088K`(29,250원)이 `tooExpensive`가
아니라 `noOrderBook`으로 나온다 — 순서를 바꾼 것이 실제로 먹는다.

테마 등락률(조회 4회):

| 테마 | 등락률에 쓴 종목 | 상승·하락·보합 | 호가 없음 |
|------|------|------|------|
| 168 우주(위성,발사체) | 18 / 19 | 18 · 0 · **0** | 1 (000880) |
| 322 건설사 | 39 / 43 | 38 · 1 · **0** | 4 |
| 039 지주 | 47 / 50 | 35 · 11 · **1** | 3 |

`039 지주`가 이 변경의 요점을 그대로 보여 준다 — 예전이라면 `보합 4`였을 자리가
**진짜 보합 1 + 호가 없음 3**으로 갈렸다. 세 테마 모두 `turnoverCount`가
`quotedCount`와 같아졌다(중앙값·평균·상승하락·가중평균 넷이 같은 표본을 본다).

### 확인하지 못한 것

- **장전(08:30 이전)·휴장일·시간외 단일가 종료 후**에 정상 종목의 호가가 어떻게
  오는지. 이 판정의 유일한 미측정 경계다. 개장일 08:00·08:45에 `probeHalted scan`을
  돌리면 잴 수 있다
- **VI 발동 중**인 종목의 호가. 표본 어디에도 `vi_cls_code != N`이 없었다
- **정리매매(`sltr_yn = Y`)** 종목. 12종목 전부 `N`이라 한 건도 못 봤다.
  정리매매는 30분 단위 단일가로 실제 체결되므로 호가가 있을 것이라 보지만 안 쟀다
- 화면(브라우저)에서 `호가 없음` 칩과 테마 카드 문구가 어떻게 보이는지 —
  타입체크·빌드만 통과시켰다

### 이번에 지킨 선

주문·정정·취소·예약주문 **전송 0건**. 자동매매 `stopped` 그대로, 리스크 룰·
`.env`·`.cache/` 무변경. 실주문 게이트는 손대지 않았다. KIS는 **시세 조회만**
했고 총 100회 남짓이다.

---

## 2026-07-31 장중 — 화면에 뜨는 전략 판정문은 러너가 도는 축에서 잰 것이 아니다

앞 절("비용 문턱이 시간비례여야 하나")이 남긴 물음 — *"러너는 분봉, 성과 측정은
일봉이라 보유 기간부터 정해야 한다"* — 을 **재서** 답한 기록이다.

### ★ 판단

**백테스트는 러너가 하지 않는 일을 재고 있다.** 화면의 `backtestNote`·`verdict`는
전부 **일봉** 축에서 잰 값인데 러너는 **1분봉**으로 신호를 낸다. 같은 8종목에
같은 전략을 두 축으로 각각 태우면 판정이 바뀐다. 특히 평균 회귀의
*"승률 70.8%로 셋 중 압도적 1위"*는 **분봉 축에서 19.6%**다. 그 문장은 자동매매
시작 버튼 바로 옆에 뜬다.

> **고침(2026-08-01).** 화면이 이제 축을 말한다. `backtestNote` 문장 하나가
> `StrategyMeasurement[]`가 되어 축(`CandleAxis`)이 **필드로** 다니고, 러너 축
> 측정이 맨 앞에 오며, 화면은 `1분봉 · 자동매매가 실제로 보는 봉입니다` /
> `일봉 · 자동매매는 이 봉으로 돌지 않습니다`로 갈라 적는다. 어느 축이 러너의
> 것인지는 서버가 준다(`StrategyListResponse.runnerAxis`).
>
> **두 축을 하나로 맞춘 것은 아니다.** `verdict`도 그대로다 — 분봉 표본이
> 8종목 × 4거래일로 얇아 그것으로 뒤집지 않았다. 축이 다르다는 사실은 표본과
> 무관하게 참이라 그것만 올렸다. 아래 「다음에 잴 것」은 그대로 남아 있다.
>
> 곁들여 걸린 것 — 전략 목록 조회가 실패하면 드롭다운과 판정문이 함께 사라지는데
> `autoStrategy`는 초기값(`ma_cross`)이라 **시작 버튼이 그대로 눌렸다.** 무엇이
> 걸리는지도 모르는 채 실계좌 매매가 시작되는 자리라 막았다.

### 코드로 확인한 것 (추측 아님)

| | 무엇을 넣나 | 어디 |
|------|------|------|
| 러너 | `getInstrumentIntradayCandles` → `FHKST03010230`, `FID_INPUT_HOUR_1='235959'`, **1분봉 최대 120개** | `trading/autoTrader.ts:418` `loadCandles` |
| 성과 측정 | `getDailyCandleHistory(..., 350)` → **일봉 350개** | `scripts/measureStrategies.ts:117` |

`minBars`(21/21/20)는 **어느 쪽에서도 강제되지 않는다.** 러너도 `backtest()`도
읽지 않고 `scripts/compareStrategies.ts`만 쓴다. 전략 안에서 봉이 모자라면
`null`을 돌려주는 것이 유일한 방어다.

### ★ KIS 창 실측 — 개장 직후에는 봉이 없고, 없으면 **전날 것이 조용히 온다**

`000020`, 실전 서버, 2026-07-31 14:1x. 제품 코드와 같은 TR·같은 인자다.

| `FID_INPUT_DATE_1` | `FID_INPUT_HOUR_1` | 받은 것 |
|------|------|------|
| 20260730 | 090100 | **2봉** (09:00~09:01, 전부 0730) |
| 20260730 | 090500 | **6봉** (09:00~09:05) |
| 20260730 | 091500 | 16봉 |
| 20260730 | 092000 | **21봉** |
| 20260730 | 093000 | 30봉 |
| 20260730 | 100000 | 60봉 |
| **20260726(일)** | 235959 | **120봉 — 전부 `20260724`** (12:55~15:30) |
| **20260725(토)** | 235959 | **120봉 — 전부 `20260724`** |
| 20260731(오늘) | 235959 | 120봉 (11:35~14:06, 전부 오늘) |

두 가지가 확정된다.

1. **날짜 경계는 단단하다.** 그 날짜에 봉이 있으면 그 날짜 것만, 장 시작에서
   잘려서 온다. 그래서 **09:05에 러너가 쥐는 것은 6봉**이고 `minBars`(20~21)를
   채우는 것은 **09:19~09:20**이다. 개장 후 20분 동안 세 전략 모두 신호를 못 낸다
   (오류가 아니라 `신호 없음`으로 조용히 지나간다).
2. **그 날짜에 봉이 하나도 없으면 KIS가 이전 거래일 것을 채워 준다.**
   `20260726`(일요일)을 물었는데 `20260724` 120봉이 왔다. 오류도 빈 응답도 아니고
   `MCA00000 정상처리`다. 이것이 *"러너 분봉이 어제 오후 2시간만 왔다"*의 정체다.
   `loadCandles`는 `candles[last].close`를 그대로 `price`로 쓰고 **날짜를 보지
   않는다.** 09:00:00~KIS가 오늘 첫 분봉을 내놓기 전 사이에는 `minBars`가 이미
   차 있으므로 **어제 15:30 종가로 신호가 나고 수량이 계산될 수 있다.** 그 구간이
   몇 초인지는 못 쟀다(개장 시각에 재야 한다).

> **고침(2026-07-31).** `loadCandles`가 **마지막 봉의 KST 날짜**를 본다. 오늘 것이
> 아니면 그 종목을 후보에서 **뺀다** — `신호 없음`이 아니라 제외로, 사유와 마지막
> 봉 날짜를 실행 기록에 남긴다. 판정은 `backend/src/trading/runCandles.ts`의
> `checkCandleDay`·`classifyCandles`이고 시험은 `runCandles.test.ts`에 있다.
>
> 시계로 가르지 않았다("장전이면 검사를 건너뛴다" 같은 것). 휴장일을 아는 것은
> 서버지 이 계산이 아니고, 시계로 가르면 휴장일에 같은 결함이 그대로 돌아온다.
> 그래서 장전·휴장일에는 **전 종목이 제외**된다 — 그 시각에 오늘 값은 존재하지
> 않으므로 그게 맞는 동작이고, 기록에 사유가 남는 것이 이 고침의 절반이다.
>
> **구간 길이는 여전히 못 쟀다.** 아래 「다음에 잴 것」에 그대로 남는다 — 이제
> 그 구간에 주문이 나가지는 않지만, 몇 초 동안 `제외`가 찍히는지는 모른다.

### ★ 같은 종목·같은 전략을 두 축으로 각각 태운 결과

**비용 포함**(`DEFAULT_COSTS` = 수수료 0.015%×2 + 거래세 0.20% + 슬리피지 0.1%×2,
왕복 **0.43%**). 현금 50,000원. 종목은 `measureStrategies`와 같은 문(가격·유동성·
왕복비용)을 통과한 앞 8종목.

- 일봉 축: 각 종목 **350봉**(2025-02~2026-07-30)
- 분봉 축: **2026-07-27 ~ 07-30 4거래일**, 일봉 고가·저가와 대조해 통과한 날만
  (총 7,238봉)

| 전략 | 축 | 매매 | 승률 | 종목 수익률 중앙값 | 플러스 | 비용/원금 중앙값 | 보유 중앙값 |
|------|------|------|------|------|------|------|------|
| 이동평균 교차 | 일봉 | 82 | 30.5% | -4.45% | 3/8 | 2.22% | 20일 |
| | **분봉** | **242** | **11.6%** | **-11.97%** | **0/8** | **4.97%** | **12분** |
| 변동성 돌파 | 일봉 | 351 | 29.1% | -25.75% | 0/8 | 8.42% | 1일 |
| | **분봉** | **1,059** | **6.4%** | **-35.12%** | **0/8** | **22.79%** | **1분** |
| 평균 회귀 | 일봉 | 73 | 68.5% | -2.80% | 4/8 | 1.97% | 14일 |
| | **분봉** | **209** | **19.6%** | **-10.00%** | **1/8** | **5.44%** | **12분** |

분봉 축에서 **24개 조합(8종목 × 3전략) 중 플러스는 1개**다. 얇은 종목이 끌고 간
결과가 아니다 — 가장 촘촘한 `000080 하이트진로`(하루 355/391봉)가 분봉 축에서
가장 나쁘다(이동평균 교차 -22.84%, 변동성 돌파 -53.53%).

### ★ 왜 갈리나 — 방향이 아니라 산수다

| 몇 봉 뒤 | 일봉 \|Δ\| 중앙값 | 분봉 \|Δ\| 중앙값 |
|------|------|------|
| 1봉 | 1.005% | 0.105% |
| 5봉 | 2.236% | 0.257% |
| 10봉 | 3.189% | 0.340% |
| 20봉 | 4.468% | 0.486% |
| 60봉 | 9.127% | 0.869% |

왕복 비용은 **0.430%**로 축과 무관하게 똑같이 나간다.

- 일봉 축은 **1봉만에** 이 레포 자신의 잣대(`비용 ≤ 폭의 50%` → 0.860% 필요)를
  넘긴다.
- 분봉 축은 **60봉(약 1시간)이 지나야** 그 잣대를 겨우 넘긴다. 그런데 실제 보유
  중앙값은 **12분**이다.
- 매매 하나하나로 세면 **매수→매도 사이 값 변화가 왕복 비용에 못 미친 매매가
  이동평균 교차 88.4% · 변동성 돌파 93.6% · 평균 회귀 80.4%**다. 변동성 돌파는
  값 변화 중앙값이 **0.000%** — 방향을 맞히고 틀리고의 문제가 아니라 동전 던지기에
  0.43%를 내고 있다.

**따라서 `aheadRangeRate`(같은 날 안에 되판다)조차 지나치게 후한 가정이다.**
실제 보유는 12분이지 하루가 아니다. 앞 절이 남긴 물음의 답은 "며칠치냐 하루치냐"가
아니라 **"지금 잣대가 재는 지평(하루)이 실제 보유(12분)보다 26배 길다"**이다.

### 곁들여 드러난 것 — 같은 파라미터가 축마다 다른 뜻이다

`movingAverage(candles, 20)`은 일봉이면 어느 종목에서나 약 4주지만, 분봉에서는
**그 종목이 하루에 몇 번 체결되는지에 따라 달라진다**(KIS 분봉은 체결이 없는 분을
건너뛴다).

| 종목 | 하루 평균 봉수 | **20봉이 덮는 시간(중앙값)** |
|------|------|------|
| 000080 하이트진로 | 355 / 391분 | 19분 |
| 000040 KR모터스 | 325 | 19분 |
| 0000D0 TIGER 엔비디아…커버드콜 | 179 | 38분 |
| 000050 경방 | 164 | 41분 |
| 0002C0 에셋플러스 인도일등기업… | 28 | **223분** |

같은 `MA(20)`이 한 종목에서는 19분선이고 다른 종목에서는 3.7시간선이다.
코드 어디에도 이 사실이 없다.

### 이 판단이 틀릴 수 있는 지점 (반증 조건)

- **분봉 표본이 8종목 × 4거래일 한 장세다.** 이 레포는 8종목 → 20종목에서 판정이
  뒤집힌 전례가 있다. 매매 수(242/1,059/209)는 두껍지만 **종목 수와 날짜 폭이
  얇다.** 20종목 × 15거래일로 다시 재서 분봉 축 중앙값이 0 위로 올라오면 이
  판정은 틀린 것이다.
- 다만 **위 「산수」 부분은 수익률보다 뒤집기 어렵다.** 분봉 10~20봉 구간의
  \|Δ\| 중앙값(0.34~0.49%)이 왕복 0.43%를 크게 웃돌아야 뒤집히는데, 그러려면
  분 단위 변동성이 지금의 2배 이상인 국면이어야 한다. **급등락 국면을 따로 갈라
  재면 그 반례가 나올 수 있다** — 이번 4거래일에는 그런 날이 없었다.
- **재구성이 실제 러너 입력과 다를 수 있다.** 러너는 최대 120봉 창을 보는데
  백테스트는 처음부터 누적된 창을 본다. 다만 세 전략 모두 마지막 20~21봉만
  쓰므로 신호는 같아야 한다 — 이게 틀렸다면 판정도 다시 봐야 한다.
- **러너는 60초마다 깨어나는데 백테스트는 모든 봉을 본다.** 봉이 러너 주기보다
  촘촘하거나 체결이 뜸해 봉이 건너뛰면 러너가 놓치는 교차가 생긴다. 그쪽 매매는
  실제로 더 적을 것이다(비용은 줄고 신호도 준다). 어느 쪽이 큰지는 안 쟀다.
- ETF 2건(`0000D0`)이 일봉 고저 대조에 걸려 4일 중 2일이 빠졌다. 앞 절에서 이미
  나온 ETF 재구성 문제이고 **원인은 여전히 모른다.**

### 검산 (norm 4)

분봉 축 백테스트 중 미청산이 없는 건에서
`|endEquity − startCash − Σ netPnl|`의 최대치가 **3.3e-11원**이다. 거래별 손익과
현금 변화가 어긋나지 않는다(매수 수수료 누락 버그 `31b6cd2`의 재발 없음).

`KR_SELL_TAX_RATE`도 확인했다 — `shared/src/index.ts`가 **0.002(0.20%)**이고
세 전략의 `backtestNote`도 "매도 거래세 0.20%(왕복 0.43%)"로 잰 것이다. 세율
때문에 낡은 판정문은 **없다.** 낡은 것은 세율이 아니라 **축**이다.

> **2026-08-12 정정 — 세율 자체는 맞았지만 "누구에게 물리나"가 틀렸다.**
> 국내 상장 ETF는 매도 시 증권거래세가 면제인데(종류 무관) 백테스트가 조건
> 없이 0.20%를 물리고 있었다. 표집틀에 `kr-etf`가 들어 있으므로 위 판정문의
> 표본에도 ETF가 섞여 있다 — **그 부분은 세율 때문에 낡았다.**
>
> 크기를 쟀다(DB에 모아 둔 일봉 394~399봉, 원금 1천만원, ETF 3종목):
>
> | 종목 | 전략 | 매매 | 수익률 (전 → 후) | 승률 (전 → 후) |
> |------|------|------|------|------|
> | TIGER 엔비디아미국채커버드콜밸런스(합성) | 변동성 돌파 | 45 | −18.53% → **−10.85%** | 26.7% → **40.0%** |
> | PLUS 한화그룹주 | 변동성 돌파 | 63 | −29.86% → **−20.46%** | 39.7% → **42.9%** |
> | RISE 바이오TOP10액티브 | 변동성 돌파 | 62 | −25.16% → **−15.30%** | 38.7% → 38.7% |
> | TIGER 엔비디아…(합성) | 이동평균 교차 | 12 | +4.12% → **+6.65%** | 33.3% → **41.7%** |
>
> 매매가 잦을수록 크게 벌어진다. **판정문의 숫자는 손으로 고치지 않았다** —
> 다시 재기 전까지 그대로 두고, `StrategyMeasurement.sample`에 "잰 조건이 지금
> 코드와 다르다"는 단서를 붙였다(`ETF_TAX_STALE_NOTE`).

### 옆에서 함께 걸린 것 (이번 물음의 답은 아니지만 같은 코드에 있다)

1. **보유 기간이 어디에도 정의돼 있지 않다.** 세 전략 모두 청산이 신호 반전
   조건뿐이다(데드크로스 / 기준선 이탈 / z ≥ 0). 장 마감 청산도, 최대 보유
   기간도, 손절도 없다. `strategy.ts:202` 주석이 *"보유를 다음 날로 넘기지
   않으려면 러너 쪽에서 장 마감 청산을 걸면 된다"*고 적고 있는데 **러너에 그
   코드가 없다.**
2. **후보에서 빠진 보유 종목은 팔 수 없다.** `runOnce`는
   `loadAutoTraderCandidates`가 준 후보의 앞 **8종목**(`MAX_CANDIDATES_PER_RUN`)만
   분봉을 받고, `positions`는 그 8종목과 겹치는 것만 만든다. 값이 오르면
   `price > cash`로 후보에서 빠지고, 얇아지면 `illiquid`로 빠진다 —
   **그 순간 매도 신호를 낼 통로가 사라진다.** 후보 통과 152종목 중 앞 8종목만
   보므로 하루 안에도 자주 일어날 수 있다.

   > **고침(2026-07-31).** 보유 종목은 후보 필터와 무관하게 분봉을 받는다
   > (`runCandles.ts`의 `candleTargets`). 보유에는 호출 예산을 걸지 않는다 —
   > 자르는 순간 못 파는 종목이 다시 생긴다. 한 회차 호출 수는
   > `max(MAX_CANDIDATES_PER_RUN, 보유 종목 수)`이고, 보유가 예산을 다 먹으면
   > 그 회차는 매도만 판단한다. 위 날짜 검사가 이 문제를 키우기 때문에 같은
   > 작업에서 함께 고쳤다. 잔고의 종목코드를 `Instrument`로 바꾸는 것은
   > `AutoTraderDeps.loadHeldInstruments`가 맡는다.
3. **기본 리스크 룰로는 러너가 주문을 한 건도 못 낸다.** 러너는 항상
   `orderType: 'market'`인데 기본값이 `allowMarketOrder: false`다.
4. **분봉 축 매매 빈도가 일일 주문 한도를 넘긴다.** 종목 **하나**만 굴려도 하루
   주문이 이동평균 교차 중앙 17.3건(최대 28.5) · 변동성 돌파 83.1건(최대 127.5) ·
   평균 회귀 14.6건(최대 21.0)이다. 한도는 20건이다. 즉 백테스트가 재는 매매를
   **러너는 애초에 다 낼 수 없다** — 세 번째 축 어긋남이다.

### 다음에 잴 것

| 무엇 | 왜 | 어떻게 |
|------|------|------|
| ~~분봉 축을 20종목 × 15거래일로 다시~~ **2026-08-01에 쟀다 — 안 뒤집혔다** | 8종목에서 뒤집힌 전례 | 아래 「2026-08-01」 절 |
| 급등락 국면을 갈라 분 단위 \|Δ\| 분포 | 위 「산수」의 유일한 반례 후보 | 지수 등락률 상·하위 날을 골라 같은 측정 |
| 09:00:00에 KIS가 오늘 첫 분봉을 언제 내놓나 | 어제 종가로 주문이 나갈 수 있는 구간의 길이 | 개장일 08:59~09:02에 `probeIntradayHistory` 반복 |
| 러너 60초 주기가 놓치는 교차 비율 | 실제 매매 수를 백테스트가 부풀리는지 | 재구성 분봉을 60초 샘플링해 같은 전략을 다시 태움 |
| 보유 기간을 정한 뒤 비용 문턱 재설계 | 앞 절이 막혀 있던 지점 | 정한 지평(예: 12분·1시간·1일)의 `aheadRangeRate`를 그 지평으로 다시 계산 |

### 쓴 도구 (스크래치패드 · 제품 코드 아님)

`/tmp/quant-probe/collect.ts`(원자료 수집) · `analyze.ts`(두 축 백테스트) ·
`analyze2.ts`(종목별·검산·보유분포·주문건수) · `analyze3.ts`(20봉이 몇 분인가) ·
`probeWindow.ts`(KIS 창 경계). 원자료 `/tmp/quant-probe/raw.json`.
**제품으로 남길 것은 `trading-dev`가 만든다.**

### 이번에 지킨 선

주문·정정·취소·예약주문 **전송 0건**. 자동매매 시작 안 함. 리스크 룰·`.env`·
`.cache/`·제품 코드·레포 시험 **무변경**(고친 것은 이 문서와
`docs/TRADING_ROADMAP.md`뿐이다). KIS는 **시세·분봉 조회만** 했고 약 **217회**다.

---

## 2026-08-01 — 분봉 축을 20종목 × 15거래일로 다시 쟀다. 안 뒤집혔다

앞 절이 남긴 첫 번째 물음 — *"8종목 × 4거래일은 얇다. 20종목으로 늘리면
뒤집히지 않겠나"* — 를 **재서** 답한 기록이다. 이 레포는 일봉 축에서 8 → 20종목에
판정이 뒤집힌 전례가 있어 그것을 계속 경계해 왔다.

### ★ 판단

**표본을 3배 이상 늘려도 분봉 축 판정은 그대로다.** 방향도 크기도 바뀌지 않았다.

| 전략 | 8종목 × 4거래일 (07-31) | **20종목 × 15거래일 (08-01)** |
|------|------|------|
| 이동평균 교차 | 승률 11.6% · 중앙값 -11.97% · 플러스 0/8 | 승률 **9.81%** · 중앙값 **-10.95%** · 플러스 **1/20** |
| 변동성 돌파 | 승률 6.4% · 중앙값 -35.12% · 플러스 0/8 | 승률 **4.38%** · 중앙값 **-24.68%** · 플러스 **0/20** |
| 평균 회귀 | 승률 19.6% · 중앙값 -10.00% · 플러스 1/8 | 승률 **15.03%** · 중앙값 **-13.25%** · 플러스 **1/20** |

세 전략 × 20종목 = 60개 조합에서 **플러스는 2개**다. 변동성 돌파는 20종목
전부에서 잃었다.

### 표본

현금 50,000원 · 스크리닝과 같은 순서의 앞 20종목 · 2026-04-29 ~ 07-31 15거래일
(구간 전체에 고르게 흩뿌림) · 왕복 비용 0.43% 포함. KIS 1,520회.

시도한 300 종목·날 중 **249건 사용**, 51건은 버렸다.

| 버린 사유 | 건수 |
|------|------|
| 일봉 고가·저가와 불일치 | 42 |
| 그 날짜에 봉이 없음 | 9 |
| 일봉이 없어 검산 못 함 | 0 |

총 51,810봉.

### ★ 고저 불일치는 ETF에 몰린다 (원인은 여전히 모른다)

앞선 측정에서 "ETF 재구성이 자주 어긋난다(750건 중 110건, 14.7%)"고만 적었는데,
이번 표본에서는 **어느 종목이 어긋나는지가 뚜렷하다.**

    0000D0 TIGER 엔비디아…      15일 중 10일 불일치
    0000H0 KODEX 인도Nifty…    15일 중  9일 불일치
    0004G0 1Q 미국배당TOP30     추가 11일
    0005C0 RISE 미국S&P500엔   추가 12일
    000020 동화약품(일반주)        15일 중  0일
    000080 하이트진로(일반주)       0일

**일반주는 0건이고 42건이 전부 ETF다.** 원인은 아직 모른다(수정주가·LP 호가·
분봉에 안 잡히는 체결 중 무엇인지). 다만 **버리는 판정 자체는 옳게 동작한다** —
검산을 통과한 것만 쓰므로 틀린 재구성이 결과에 섞이지는 않는다.

### ~~★ 새로 안 것 — 밤을 넘긴 매매는 4~7%뿐이다~~ **이 절은 틀렸다 (2026-08-01 저녁 정정)**

세 전략 모두 청산이 신호 반전뿐이고 장 마감 청산이 없다. 그래서 여러 날 분봉을
이어 붙이면 15:30 → 다음 개장의 갭이 한 봉으로 들어간다.

| 전략 | 밤을 넘긴 매매 |
|------|------|
| 이동평균 교차 | 77 / 1,162회 (6.63%) |
| 변동성 돌파 | 26 / 3,336회 (0.78%) |
| 평균 회귀 | 45 / 1,018회 (4.42%) |

> ### ★ 정정 — **건수로만 봤고, 그래서 결론이 틀렸다**
>
> 원래 여기에 *"보유 중앙값이 1~14분이라 대부분 하루 안에서 끝난다. **이어 붙인 것이
> 결과를 만든 것이 아니다**"*라고 적혀 있었다. **건수만 보고 낸 결론이다.**
>
> **손익으로 세면 밤 넘김이 전체 손실의 56~162%를 만든다.** 건수는 작아도 건당
> 크기가 20~200배라 지배한다.
>
> | 전략 | 밤 넘김 건당 | 당일 건당 | 밤이 만든 손실 비율 |
> |------|------|------|------|
> | 이동평균 교차 | -131bp | -7.4bp | **56%** |
> | 변동성 돌파 | -151bp | -0.7bp | **58%** |
> | 평균 회귀 | -120bp | **+2.1bp** | **162%** |
>
> **그리고 그 손실은 시장이 아니라 표본 설계에서 나온 인공물이다.** 수집기가
> `spreadEvenly`로 3개월에 15일을 흩뿌렸기 때문에, 이어 붙인 자리 229곳의
> **달력 간격이 중앙값 7일**(최대 53일)이었다. 그 자리의 갭은 **|Δ| 중앙 3.30%**로
> 왕복 비용 0.43%의 **7.7배**다. 백테스트는 그걸 "한 봉"으로 먹는다.
>
> **쓰임이 다르면 표본도 달라야 한다.** 흩뿌리기는 여러 장세를 봐야 하는 **분포
> 측정**에 맞고, 이어 붙여 백테스트하려면 **연속**이어야 한다. `collectMinuteCandles.ts`에
> `spread | contiguous`를 두고 어느 쪽인지 `summary`에 남긴다.
>
> 「장 마감 청산이 좋아 보인다」는 관측도 대부분 이 인공물을 떼어낸 것이라
> 근거로 쓸 수 없다.

### 앞선 측정과 갈린 지점 — 「비용에 못 미친 매매」 비율

| 전략 | 8종목 × 4거래일 | 20종목 × 15거래일 |
|------|------|------|
| 이동평균 교차 | 88.4% | **60.2%** |
| 변동성 돌파 | 93.6% | **85.2%** |
| 평균 회귀 | 80.4% | **70.7%** |

세 전략 모두 낮아졌다. 표본 기간이 4거래일에서 3개월로 넓어져 변동이 큰 날이
섞인 것으로 보이지만 **그것까지 가르지는 않았다.** 그래도 여전히 매매의 60~85%가
왕복 비용도 못 덮는다.

### `verdict`는 바꾸지 않았다

분봉 축에서는 셋 다 `no_edge`에 가깝다(플러스 2/60). 그런데 코드의 `verdict`는
**일봉 축에서 정한 값**이고 두 축은 아직 하나로 맞춰지지 않았다. 축이 다른 측정
결과로 판정을 뒤집으면 그다음에 "이 판정이 어느 축의 것인가"를 다시 알 수 없게
된다. 측정 스크립트도 그렇게 말하고 넘어간다.

**남은 물음은 "어느 축의 `verdict`를 화면에 걸 것인가"다.** 화면은 자동매매를
켜는 자리이고 자동매매는 분봉으로 돈다.

### 쓴 도구 (이제 제품 코드다)

`scripts/collectMinuteCandles.ts`(받는다) → `scripts/measureStrategiesIntraday.ts`
(받아 둔 것만 읽어 잰다). 날짜 지정 분봉 조회는 `kis/rest.ts`의
`getDomesticDayMinuteCandles`로 옮겼다. **앞선 측정은 스크래치패드 도구로 해서
다시 잴 수 없었다** — 그게 이 절을 쓸 수 있게 된 이유다.

### 이번에 지킨 선

주문·정정·취소·예약주문 **전송 0건**. 자동매매 시작 안 함. 리스크 룰·`.env`·
`.cache/` **무변경**. KIS는 **시세·분봉·잔고 조회만** 했다.

---

## 2026-08-01 저녁 — 모의투자를 하려다 배관이 다 드러났다

사용자가 "백테스트에서 유의미한 데이터를 근거로 모의투자를 진행하자"고 해서
`/trading-loop`을 한 바퀴 돌린 기록이다. **모의투자는 아직 못 했고**, 대신 그 앞을
막고 있던 것들이 드러났다.

### ★ 비용을 전부 0으로 놔도 이기지 못한다 (측정, 이번 바퀴에서 가장 큰 것)

그동안 "왕복 0.43%가 분봉 1봉 변화(0.105%)보다 커서 진다"고 설명해 왔다. 슬리피지
0.1%/편이 한 번도 측정된 적 없는 **가정**이라, 그 가정이 판정을 만든 것인지 확인했다.

| 전략 | 왕복 0.430%(지금) | 0.230%(슬리피지 0) | **0.000%(NO_COSTS)** |
|------|------|------|------|
| 이동평균 교차 | -10.95% · 1/20 | -7.07% · 2/20 | **-0.80% · 5/20** |
| 변동성 돌파 | -24.68% · 0/20 | -13.60% · 0/20 | **-0.08% · 5/20** |
| 평균 회귀 | -13.25% · 1/20 | -5.90% · 2/20 | **0.00% · 8/20** |

**비용이 0인 세상에서도 중앙값이 0 이하이고 플러스가 5~8/20이다.** 이 축에서 세
전략은 동전 던지기다. **비용은 지는 속도를 정할 뿐 승패를 만들지 않았다.**

곁들여: 표본 20종목의 1틱이 값의 **0.057%**(중앙값)이고, 51,790봉 중 **36.8%가 한 푼도
안 움직였고 38.6%가 1틱 미만**이었다. 슬리피지 0.1%/편 ≒ 1.75틱은 과소가 아니라 보수적이다.

### ★ 「60초 주기가 놓치는 교차」 — **0이다** (미측정 항목 해소)

`docs/USER_FINDINGS.md`가 「다음에 잴 것」으로 남겨 뒀던 항목이다. 제품 `backtest()`와
체결·비용·수량을 똑같이 두고 **평가하는 봉만** 러너 주기로 제한해 쟀다.

| 주기 | 평가한 봉 | 세 전략 결과 |
|------|------|------|
| 모든 봉 | 51,790 | (지금의 측정값) |
| **60초(러너)** | **51,790 (100.0%)** | **소수점까지 동일** |
| 60초·위상 30초 | 100.0% | 동일 |
| 120초 | 59.2% | 매매 줄고 중앙값 개선 |

산수다 — KIS 1분봉은 1분에 최대 하나이므로 60초마다 깨어나면 닫힌 봉을 건너뛸 수 없다.
**120봉 창 제한도 차이가 0원이었다.** 세 차이 중 둘이 차이가 아니었다.

다만 러너가 **회차를 거르면**(`busy`) 120초 줄이 그 모습이 된다. 그건 실측 대상이다.

### ★ 계좌 하나로 굴리면 화면 판정문보다 훨씬 나쁘다

| | 종목별 독립 백테스트(화면 값) | 계좌 하나 시뮬(러너가 하는 일) |
|------|------|------|
| 이동평균 교차 | -10.95% | **-55.05%** |
| 변동성 돌파 | -24.68% | **-46.23%** |
| 평균 회귀 | -13.25% | **-40.52%** |

같은 표본·기간·비용이다. 갈리는 이유는 전략이 아니라 **자본 배치**다 — 백테스트는
20종목에 각각 50,000원을 따로 주고, 러너는 **하나의 50,000원**에 몰아친다.
값이 한 푼도 안 움직였다고 가정하고 비용만 물리면 손실의 **79~117%**가 설명된다.

### ★ 일일 20건 한도가 매도를 막는다

`getTodayUsage`(`db/riskRules.ts`)가 매수·매도를 가리지 않고 `place`를 센다. 매수가
한도를 먼저 먹으면 **그날은 팔 수 없다.** 시뮬에서 `maxPositions=1`에 매도 65회,
`maxPositions=3`에 120~2,219회가 막혔다.

리스크 룰은 손실을 줄이려고 있는데 여기서는 **출구를 잠근다.** `runCandles.ts`의
「나가는 문이 들어오는 문보다 앞이다」 원칙이 여기서는 안 지켜진다. **고칠지는 판단이
필요하다** — 지금은 이 상호작용을 실험으로 관측하는 쪽으로 남겨 뒀다.

### 모의 환경에서 막고 있는 것 셋 (실측)

| 무엇 | 확인한 것 |
|------|------|
| 계좌가 안 붙는다 | 8자리 계좌번호 × 상품코드 7종 전부 `INVALID_CHECK_ACNO`. **앱키는 유효하다** — 모의 서버에서 토큰 발급·시세 조회가 된다. 실계좌 앱키는 `EGW02007`로 **다른 코드**로 거부되므로 앱키/계좌를 갈라 볼 수 있다 |
| `allowMarketOrder: false` | 네 계좌 전부. 러너는 항상 시장가라 **게이트가 열려도 주문을 한 건도 못 낸다** |
| **모의 서버에 `chk-holiday`가 없다** | `EGW02006 모의투자 TR 이 아닙니다`. `riskRules.ts`의 `catch`가 `개장일을 확인할 수 없어 주문을 보류합니다`를 넣는다 — **월요일 장중에도 똑같다.** 즉 모의 환경에서는 리스크 룰을 통과하는 주문이 존재할 수 없다 |

세 번째가 이번에 새로 드러난 것이다. 앞의 둘을 풀어도 이것이 남는다.

곁들여 잰 것: 러너 한 회차는 KIS **17회**(잔고 1 + 멀티시세 8 + 분봉 8)이고 60초
주기면 정규장에만 6,630회다. **러너는 장 밖에서 멈추지 않아** 24시간 켜 두면 24,480회다.
그리고 **vts 멀티시세는 8묶음 중 1~3묶음이 `EGW00201`로 조용히 실패한다**(실전은
10묶음 300종목에 0건이었다) — 매 회차 후보의 12.5~37.5%를 잃는다.

### ★ 모의 앱키가 실전 서버에 조용히 붙었다 (2026-08-01 실측)

`.env`가 `KIS_PRIMARY_ACCOUNT_ID=VTS`인 채로 `APP_ENV=prod`로 돌린 것뿐인데 짝이
어긋났고, **아무도 그 사실을 말해 주지 않았다.**

```
APP_ENV=prod · restBase=https://openapi.koreainvestment.com:9443
primary 자격증명 = VTS-EXTRAORDINARY   ← 모의용 앱키인데 서버는 실전이다
```

| 무엇 | 결과 |
|------|------|
| 멀티시세·일봉 | **정상 응답했다** |
| 분봉 | `EGW02004`로 막힘 |
| 토큰 | `backend/.cache/token-prod-VTS-EXTRAORDINARY.json`이 **실제로 생겼다** |

**반쯤 통과한 것이 핵심이다.** 통째로 깨졌다면 바로 알았을 텐데, 값이 오니까
모의로 돌고 있다고 믿게 된다. 발급 횟수 제한이 있는 토큰까지 다른 서버에서 하나 썼다.

그리고 **주문 가드가 이 상황에서 발동하지 않았다.** `kisPost`가
`orderServerMismatch(credentialServer(credentials), config.env)`로 막는데,
`toCredentials`가 `server`를 안 채워서 그 값이 언제나 `config.env`였다 — 도달할 수 없는
방어선이었다.

고친 방식은 `docs/ARCHITECTURE.md`의 「앱키가 어느 서버용인지는 사람이 적는다」 절에 있다.
**코드는 앱키가 어느 서버용인지 알 수 없다**(KIS가 알려주지 않는다). 그래서 사람이
`KIS_<id>_SERVER`에 적게 하고, 적었으면 지킨다. 안 적으면 지금 그대로다.

### 고친 것

- `5467a97` 시장가 주문의 일일 금액 한도. 러너 주문이 **영원히 0원**으로 쌓이고 있었고,
  수동 주문은 현재가 조회 실패 시 `price: 0`으로 **1회 한도까지** 지나갔다.
- `44d3c73` 한 앱키에 계좌가 둘이면 종류를 붙인 쪽이 정규식에 안 걸려 통째로 무시됐다.
- `055e6d6` 계좌번호 자릿수가 안 맞으면 아무 말 없이 사라졌다.

### 아직 안 한 것

**모의투자 자체.** 위 셋이 풀려야 한다. 실험을 한다면 목적은 **손익이 아니라 배관**이다 —
러너는 지금까지 KIS로 주문을 **0건** 보냈고 `VTTC0012U` 경로는 한 번도 눌린 적이 없다.
3개장일이면 충분하고, 그 이상은 "모의에서 -30% 났다"가 성적처럼 읽히기 시작한다.

**모의투자가 원리적으로 못 재는 것**도 못 박아 둔다 — 슬리피지·시장충격. 모의 체결은
내 주문이 호가를 밀지 않는다고 가정한다. **"모의에서 벌었다"는 실계좌 성적의 근거가
될 수 없다.**

### 이번에 지킨 선

주문·정정·취소·예약주문 **전송 0건**. 자동매매 시작 **0회**. 리스크 룰 **무변경**
(`GET`·`SELECT`만). `.env`·`.cache/` **무변경** — 계좌번호도 내가 고치지 않았다.

---

## 2026-08-01 저녁 — 연속 표본으로 다시 쟀다. 갭을 없애도 여전히 진다

앞 절의 정정(밤 넘김을 건수로만 봤다)에서 나온 물음 — *"그 갭이 인공물이라면,
연속 표본으로 다시 재면 좋아지지 않겠나"* — 를 **재서** 답한 기록이다.

### ★ 판단 — 거의 안 나아졌다. 손실은 표본 인공물 때문이 아니다

| 전략 | 흩뿌림(정정 후) | **연속 15거래일** |
|------|------|------|
| 이동평균 교차 | -20.43% · 플러스 1/15 | **-18.53% · 0/15** |
| 변동성 돌파 | -37.22% · 0/15 | **-36.86% · 0/15** |
| 평균 회귀 | -18.30% · 1/15 | **-14.08% · 1/15** |

1~4pp만 움직였고 **플러스 종목은 오히려 줄었다**(이동평균 교차 1/15 → 0/15).

**측정 전 예측이 빗나갔다.** 갭이 손실의 56~162%를 만든다는 앞선 측정을 근거로
"연속으로 바꾸면 크게 나아질 것"이라고 적었는데, 그 56~162%는 **비용 0 기준**이었다.
비용 포함 손실에서는 비용이 73~87%를 차지한다 — **갭을 없애도 비용이 남는다.**

### 표본

현금 50,000원 · 스크리닝 순서 앞 20종목 중 **그 현금으로 실제 살 수 있는 15종목** ·
**2026-07-10 ~ 07-31 연속 15거래일** · 일봉 고저 대조를 통과한 240/300 종목·날 ·
49,284봉 · 왕복 0.43% 포함 · KIS 1,520회.

버린 60건: 고저 불일치 42(대부분 ETF) · 봉 없음 18.

### 앞선 측정과 무엇이 달라졌나

| | 고치기 전 | 고친 뒤 |
|------|------|------|
| 표본 종목 | 20종목 (못 사는 5종목이 0.00%로 섞임) | **살 수 있는 15종목** |
| 날짜 | 3개월에 흩뿌림 (간격 중앙 7일) | **연속 15거래일** |
| 이동평균 교차 | -10.95% | **-18.53%** |
| 변동성 돌파 | -24.68% | **-36.86%** |
| 평균 회귀 | -13.25% | **-14.08%** |

화면 판정문이 **실제보다 1.4~1.9배 좋게** 적혀 있었다. 두 결함을 고치고 다시
재서 갱신했다(`strategy.ts`). 추정으로 조정하지 않았다.

### 청산 규칙 — 이길 수는 없고 덜 잃을 수만 있다

같은 원자료로 청산 규칙 여섯을 훑었다(스크래치패드).

| 규칙 | 결과 |
|------|------|
| 손절 · 익절 · 트레일링 · 최대 보유 | **아무것도 안 바꾼다.** 보유 중앙값 1~13분이라 걸릴 자리가 없다. 변동성 돌파는 손절 -0.5%~-5%가 전부 같은 값 — 한 번도 발동하지 않는다 |
| **최소 보유 시간** | **유일하게 바꾼다.** 391분(하루 1회)까지 늘리면 -2.67%/-4.99%/-3.03% |

**그런데 비용을 0으로 놓으면 최소 보유가 아무것도 개선하지 않고 플러스 종목이
오히려 준다**(7→3, 6→3, 10→5). 매매 1건의 **비용 전** 수익률이 어떤 규칙에서도
0을 유의하게 못 넘는다(최선 t=0.65). 즉 **덜 잃는 법이지 이기는 법이 아니다.**

모양도 봉우리 없이 단조롭다 — "덜 매매할수록 덜 잃는다"는 뜻이고 극한은
"매매하지 마라"다. 앞뒤 구간으로 갈라도 방향은 유지되지만 **값 하나는 유지되지
않는다**(평균 회귀는 앞 구간 1위가 뒤 구간 5위).

### ★ 그래도 최소 보유 60~120분을 걸 근거가 있다 — 손익이 아니라 배관이다

종목 **하나**당 하루 주문 수(왕복 × 2):

| 최소 보유 | 이동평균 교차 | 변동성 돌파 | 평균 회귀 |
|------|------|------|------|
| 없음 | 12.4건 | **49.7건** | 11.0건 |
| 60분 | 5.0건 | 7.6건 | 5.3건 |
| 120분 | 3.6건 | 4.3건 | 3.7건 |

일일 한도가 20건이고 계좌 전체 합산이다. `maxPositions=3`이면 3배다.
**60~120분이 세 전략 모두를 한도 안에 들어오게 하는 최소선**이고, 이건 손익
주장이 아니라 배관 주장이라 표본 흔들림에 안 무너진다. 한도가 차면 **매도가
잠긴다**는 것도 이미 관측됐다.

> **구현했다** — `AutoTraderConfig.minHoldMinutes`(리스크 룰이 아니다) · 판정은
> `backend/src/trading/minHold.ts`의 순수 함수 · 매수 시각은 `trading_broker_orders`의
> 마지막 매수 접수 시각 · **기본값 0(끔)**이고 화면에서 고른다. 매수 기록이 없으면
> 미루지 않고 판다. 설계와 위험은 `docs/TRADING_API.md`의 「자동매매 최소 보유 시간」
> 절과 `docs/ARCHITECTURE.md`에 있다. **이 표는 여전히 손익 주장이 아니다.**

### 권하지 않는 것

**손절·익절·트레일링·최대 보유를 넣지 않는다.** 이 축에서 발동하지 않고, 발동하면
포지션을 비워 재진입을 열어 **매매만 늘린다**(평균 회귀 최대 3분: 매매 981 →
1,876회, 비용 8.06% → 18.14%, 중앙값 -15.13% → -29.42%).

손절을 넣지 말라는 것이 위험 관리를 하지 말라는 뜻은 아니다. **이 축에서는 손절이
발동할 만큼 값이 움직이기 전에 신호 반전이 먼저 난다**는 실측이다. 지평을 일봉으로
옮기면 달라질 수 있고 그건 다시 재야 한다.

### 이 판단이 틀릴 수 있는 지점

- **15종목의 중앙값은 8번째 한 종목이다.** 사분위 폭이 66pp라 1~3pp 차이는 그
  안에서 의미를 갖지 않는다.
- **표본 20종목이 종목코드 오름차순 앞쪽이다.** 대표성이 없고, 3개월 값 변화가
  플러스인 것이 3/15뿐인 하락 표본이다. 상승 구간에서 다시 재야 한다.
- **종목별 독립 백테스트라 낙관 쪽이다.** 계좌 하나로 굴리면 -40~-55%였다.
- **ETF 42건이 고저 대조에서 걸러졌고 원인은 아직 모른다.**

### 이번에 지킨 선

주문·정정·취소 **전송 0건**. 자동매매 시작 **0회**. `.env`·`.cache/` 무변경.
KIS는 **시세·분봉 조회만** 1,520회.

---

## 2026-08-01 밤 — 마지막 실마리는 전략 우위가 아니라 **호가 한 칸**이었다

앞 절이 남긴 물음 — *"평균 회귀 L 티어의 비용 전 +0.0887%(t=4.38, n=820)는 이 레포에서
처음 나온 유의한 플러스다. 어디서 오는가"* — 를 **재서** 답한 기록이다.

### ★ 판단 — 1틱이다. 그리고 러너는 그것을 받는 게 아니라 낸다

`+0.0887%`의 정체는 매매 1건당 **정확히 1틱**이다. 네 표본·두 기간·서로 다른 종목에서
중앙값이 **0.97~1.03틱**으로 같다. 종목별로도 11종목 중 9종목이 1.00~1.06틱이다.
값의 움직임이 아니라 **호가 격자의 눈금**이다.

**결정적 검증 넷** (내가 직접 재현했다):

| 검증 | 결과 |
|------|------|
| **거울 전략** (z≥+1.5에 사서 z≤0에 판다) | 중앙 **-1.06틱**. 부호만 뒤집히고 크기가 같다 — **방향과 무관하다** |
| 체결 5봉 지연 | +0.0887%(t=4.38) → **+0.0043%(t=0.22), 중앙 0틱**. 사라진다 |
| 가짜 진입 (조건 없이 같은 시각·같은 보유) | 중앙 **0.00틱** (n=16,337) |
| 호가 중간(mid) 기준 체결 | 중앙 **정확히 0.00틱**. 비용 한 푼 안 넣고도 우위가 0 |

### ★ 러너가 실제로 겪는 값 — 부호가 반대다

`autoTrader.ts`는 항상 `orderType: 'market'`이다. **시장가는 테이커라 스프레드를 건너간다.**

| 체결 가정 | 비용 전 평균 | 중앙(틱) |
|------|------|------|
| 지금 백테스트(체결가 그대로) | +0.0887% (t=4.38) | 1.03틱 |
| 반 칸 불리 (호가 중간 기준) | -0.0313% (t=-1.54) | 0.00틱 |
| **한 칸 불리 (스프레드 건너감) — 러너** | **-0.1511% (t=-7.38)** | **-0.96틱** |

슬리피지 가정을 **아예 빼고** 수수료·세(0.23%)만 물려도 **-0.3811%**다.
**비용 가정이 결과를 만든 것이 아니다.**

### 0.43%를 넘는 조건은 없다

사전에 정한 격자 `entryZ 5 × 시간대 7 = 35칸` 중 n≥50인 23칸에서 **0.429%를 넘은 칸은
0개**다. 최고가 `z-1.5 × 15:00~15:30`의 +0.3057%(n=63)인데, 그 시간대 스프레드가
2틱이라 그렇고 테이커가 되면 **정확히 0틱**이다.

시간대 모양은 **표본 밖에서 안 남았다** — 같은 20종목·겹치지 않는 기간에서
09:00~09:30이 +0.394% → **-0.095%**로 부호가 뒤집힌다.

### ★ 곁들여 — 이 레포의 `NO_COSTS` 측정은 1틱만큼 후하다

「비용을 전부 0으로 놔도 이기지 못한다」는 결론은 유지된다. 다만 그 측정의 `NO_COSTS`
값 자체가 **왕복 1틱(L 티어 0.129%)만큼 낙관 쪽**이다 — 봉 시가 체결은 매수를
매도호가 쪽으로, 매도를 매수호가 쪽으로 잡지 않기 때문이다.

**`DEFAULT_COSTS`의 슬리피지 0.1%/편을 낮추자는 제안이 나오면 이 근거로 막는다.**
L 티어 기준 0.1%/편은 **0.78틱/편**으로, 스프레드 한 칸을 건너는 데 필요한 양보다
오히려 **작다.**

### 표본

11종목(전부 개별주, ETF 0) × 7거래일 2026-07-23~07-31, 매매 820건.
교차 표본: 20종목 × 15거래일 2026-07-10~07-31, 매매 1,515건(겹치지 않는 앞 8일 785건).
훑은 칸 약 190개(사전 격자 35칸 포함). 검산: 시뮬이 제품 `backtest()`와 **매매
2,664건 전부 시각·체결가 완전 일치**. KIS 호출 **0회**.

### 이 판단이 틀릴 수 있는 지점

- **지정가로 대기 체결이 실제로 되면** +1틱을 받는 쪽이 된다. 다만 미체결일 때 값이
  불리하게 달아나는 선택편향이 있고, **리스크 룰·러너 구조 변경을 전제하므로 별건이다.**
- **스프레드를 직접 재지 않았다.** 틱은 KRX 규정표에서 가져왔고 스프레드=1틱이라고
  **가정**했다. 이것이 이 판단의 유일한 미측정 전제다.
- 기간이 7~15거래일이고 두 표본이 0723~0731에서 겹친다.
- L 티어 11종목은 유동성 상위 편향(일평균 거래대금 41억~693억)이다. 스프레드가 넓은
  종목은 **더 나쁠 뿐이지 좋아질 이유는 없다.**
- **저가주(1틱이 0.5~1%인 2,000원 미만) 축은 한 종목도 없다.** 틱이 커서 비용보다 큰
  우위가 산술적으로 가능한 유일한 자리인데 안 쟀다.

### 이번에 지킨 선

주문·자동매매 **0건**. 제품 코드·리스크 룰·`.env`·`.cache/` 무변경. KIS 호출 **0회**.

---

## 2026-08-01 밤 — 일봉 축 측정이 종목코드 앞쪽만 보고 있었다

분봉 축에서 고쳤던 표본 결함이 **일봉 축에 그대로 남아 있었다.** 같은 결함이 두 축에
따로 있었던 것은 그 계산이 두 스크립트 안에 각각 인라인으로 있었기 때문이다.

### ★ 판단 — 유니버스가 시장을 대표하지 않았다. 크기는 가장 최근 구간에서 가장 크다

`measureStrategies.ts`의 `pickUniverse()`가 `getCategoryInstruments(...)` 목록의 **앞쪽
60종목**을 훑고 있었다. 그 조회는 검색어가 없으면 `ORDER BY symbol`이라, 실제로 본
것은 `000020 동화약품`으로 시작하는 **종목코드가 작은 순 60개**였다.

같은 날 같은 절차(`backtestWindows` · 현금 5만원 · 왕복 0.43% · 3구간)로 두 표본을
나란히 쟀다. **갈리는 것은 오직 "누구를 쟀나"다.**

| 전략 | 옛 유니버스 (앞 60종목 중 통과 35) | **거래대금 5층 195종목** |
|------|------|------|
| 이동평균 교차 | -4.33 / 0.00 / **-4.20** · 승률 24.2% | -0.52 / -1.28 / **-6.35** · 승률 26.0% |
| 변동성 돌파 | -6.03 / -2.82 / **-7.79** · 승률 34.7% | -3.11 / -3.70 / **-7.92** · 승률 36.8% |
| 평균 회귀 | +3.88 / +2.03 / **-2.89** · 승률 72.5% | +2.91 / +1.70 / **-5.33** · 승률 67.4% |

**가장 최근 구간(3구간)이 세 전략 모두에서 더 나쁘다.** 평균 회귀는 1.8배다.
그리고 이동평균 교차는 새 표본에서 **세 구간이 모두 마이너스**가 되어 측정
스크립트가 `no_edge`에 가깝다고 알린다 — 코드 판정은 `unproven`이다.
**판정은 바꾸지 않았다.** 이 스크립트는 어긋난 것만 알리고 사람이 정한다.

### 어떻게 고쳤나 — 거래대금 층화 표집

```
표집틀(DB 활성 국내 주식·ETF·ETN 4,032종목)
  → 러너와 같은 필터(verdictFor)로 거르기 → 2,694종목
  → 거래대금 내림차순 5층(층당 약 539종목) → 층마다 등간격 40종목 → 200종목
```

거래대금을 층의 기준으로 삼은 것은 그것이 **러너 자신이 보는 축**이라서다 —
`screenQuote`의 유동성 문턱이 거래대금이고, 체결 비용(스프레드 한 칸)이 갈리는 축도
유동성이다(앞 절). 시가총액이나 업종으로 나누면 러너와 무관한 축이 된다.

| 층 | 거래대금 | 층 크기 | 뽑음 | 주식 / ETF·ETN |
|------|------|------|------|------|
| 1 | 2조 2,187억 ~ 29.5억 | 539 | 40 | 22 / 18 |
| 2 | 29.4억 ~ 8.2억 | 539 | 40 | 30 / 10 |
| 3 | 8.2억 ~ 3.1억 | 539 | 40 | 31 / 9 |
| 4 | 3.1억 ~ 1.3억 | 539 | 40 | 34 / 6 |
| 5 | 1.3억 ~ 0.5억 | 538 | 40 | 33 / 7 |

**상한은 종목 수가 아니라 KIS 호출 수로 잡는다.** 시세는 멀티시세 30종목이 1회
(`MAX_SCREEN_CALLS = 140`), 일봉 350봉은 `getDailyCandleHistory`가 130달력일씩 나눠
받아 **종목당 최대 5회**(`MAX_HISTORY_CALLS = 1000` → 표본 200종목 상한)다. 종목
수로 적어 두면 몇 회가 나가는지 알 수 없다 — 이번 본측정에서 시세는 **정확히
135회**(응답이 세어 준다), 일봉은 **200종목 × 최대 5회 = 1,000회 이하**다.
**일봉 쪽은 실제 페이지 수를 세지 않았다** — `getDailyCandleHistory`가 몇 페이지를
불렀는지 돌려주지 않아 상한만 안다. 350봉이면 보통 4페이지이므로 800회 근처로
본다(어림).

### ★ 매매 0건·미청산이 결과에 안 보이던 것 (분봉 쪽에는 이미 있던 규칙)

| 무엇 | 어떻게 | 이번 표본에서 |
|------|------|------|
| 봉이 모자람 | 표본에서 **뺀다** | 585 중 2 |
| 현금으로 1주도 못 삼 | 표본에서 **뺀다** | 585 중 7 |
| 매매 0건 | **세기만 한다** | 576 중 19~52 |
| 미청산으로 끝남 | **세기만 한다** | 576 중 **107~252** |

앞의 둘을 빼는 이유는 `backtest()`가 그 표본을 **매매 0건 · 수익률 정확히 0.00%**로
돌려주기 때문이다. 전부 마이너스인 분포에서 0은 맨 위라 중앙값을 끌어올린다.
매매 0건은 뺄지 판단이 갈리므로 **빼지 않고 센다** — 몇 건인지 안 보이는 것이 제일 나쁘다.

판정은 이제 `trading/measurementSample.ts` 한 곳이 한다. **두 측정기가 같은 규칙을
쓰고 시험도 거기 붙는다** — 인라인으로 있던 동안 분봉 쪽만 고쳐졌던 것이 이 결함의 뿌리다.

### ★ `backtest()`의 승률은 「판 것들만」의 승률이다 (동작은 안 바꿨다)

`trades[]`에는 청산된 매매만 들어간다. 미청산은 `openQuantity`/`openValue`로만 나온다.
즉 **"회복하면 판다"는 전략에서는 회복 못 한 매매가 통째로 빠진다.**

이번 표본에서 그 크기가 보인다 — 평균 회귀는 **578표본 중 252개(43.6%)가 들고
끝났고**, 그 손익은 승률 67.4%에 한 푼도 들어 있지 않다. 앞선 측정에서 구간 끝
강제 청산을 넣으면 매매 1건당 수익률이 `+1.858%(t=2.69) → +0.192%(t=0.21)`로
우위가 사라졌다.

**동작은 그대로 뒀다**(강제 청산 여부는 별건이다). 대신 세 자리에 사실을 적었다 —
`BacktestResult.winRate` 타입 주석 · `docs/ARCHITECTURE.md` · 두 측정 스크립트의
출력(미청산 표본 수를 승률과 나란히 찍는다).

### 이 판단이 틀릴 수 있는 지점

- **작업 지시에 적혀 있던 389종목 측정값을 재현하지 못했다.** 그쪽은 평균 회귀
  3구간이 -12.20%·승률 44.4%, 이동평균이 -5.33%·승률 5.2%였는데, 내 195종목
  층화 표본은 -5.33%·67.4%와 -6.35%·26.0%다. **방향은 같지만 크기가 다르다.**
  그 389종목이 어떻게 뽑혔는지 모르므로 어느 쪽이 무엇을 재는지 갈라 두지 못했다.
- **표본이 하루치 시세로 골라졌다.** 유동성 층은 2026-07-31 하루의 거래대금으로
  나눈 것이라, 그날 특별히 거래가 몰린 종목이 위층에 올라간다.
- **층 안에서 등간격으로 뽑는 것은 무작위가 아니다.** 거래대금 순위에 주기적인
  구조가 있으면 편향이 남는다. 그런 구조가 있는지는 안 쟀다.
- **일봉 축은 러너가 도는 축이 아니다.** 러너는 1분봉으로 돈다(앞 절들).
- 중앙값 1~2pp 차이는 종목별 분산 안에서 의미를 갖지 않는다.

### 이번에 지킨 선

주문·정정·취소·예약주문 **전송 0건**. 자동매매 시작 **0회**. 리스크 룰 무변경.
`.env`·`.cache/` 무변경 — 토큰도 새로 발급하지 않고 `token-prod-21.json`(10:12자)을
그대로 썼다. KIS는 **시세·일봉 조회만** 했고 세 번 돌렸다(본측정 · 표본 5종목
예행 · 옛 유니버스 A/B). 시세 330회 + 일봉 1,150회 이하다.

---

## 2026-08-01 밤 — 저가주 축에도 우위가 없다. 그리고 **이 축을 연 전제가 틀렸다**

분봉·일봉 두 절이 모두 "다음에 잴 것"으로 남긴 **한 번도 안 재 본 축**이다.

### ★ 전제부터 틀렸다 — 저가주는 틱이 큰 곳이 아니라 **작은 곳**이다

이 축을 열 때 *"2,000원 미만은 1틱이 값의 0.5~1%"*라고 적었다. **실측은 정반대다.**
2023-01 호가 개편 이후 2,000원 미만의 틱은 **1원**이라, 상대 틱이 전 구간에서 가장
작은 자리가 거기다.

| 값 구간 | 종목 | 1틱/값 중앙 | **왕복 총비용**(수수료 0.03% + 세 0.20% + 테이커 1틱) |
|------|------|------|------|
| ~999원 | 186 | 0.131% | 0.361% |
| **1,000~1,999** | 418 | **0.064%** ← 최소 | **0.294%** ← 최저 |
| 2,000~4,999 | 721 | 0.164% | 0.394% |
| 5,000~19,999 | 772 | 0.115% | 0.345% |
| 20,000~49,999 | 257 | 0.165% | 0.395% |

격자는 2,000원에서 **위로** 튄다 — 1,999원 0.050% → 2,000원 0.250%(**5배**).
0.5%가 되려면 값이 **200원 이하**여야 하는데 그런 종목은 census에 6개뿐이고 **그중
5개가 거래대금 0**이다. 즉 "왕복만 1~2%"가 아니라 **0.29~0.40%**다.

### ★ 그런데 스프레드를 재 보니 저가의 비용 우위가 사라진다

`USER_FINDINGS`가 "이 판단의 유일한 미측정 전제"로 남겨 둔 자리다. 호가창을 직접
받지 못해 **Roll 추정량**(체결가 변화의 1차 자기공분산)을 분봉에 적용했다 —
**추정이지 실측이 아니다.**

| | 실효 스프레드 | 틱 대비 | 총 왕복(0.23% + 스프레드) |
|------|------|------|------|
| 저가 500~1,999원 (16종목) | **0.315%** | 3.45배 | **0.545%** |
| 대조 5,000~19,999원 (8종목) | **0.311%** | 2.62배 | **0.541%** |

**틱으로 보면 저가가 싼데 실효 스프레드로는 똑같다.** 독립 방증 — 1분 안에 값이
움직였을 때의 중앙 이동이 저가 0.229%(2.0틱) · 대조 0.204%(2.0틱)로 **% 기준으로
거의 같다.** 시장은 알갱이를 틱이 아니라 **%로** 정한다.

★ **왕복 0.545%/0.541%는 `DEFAULT_COSTS`(0.43%)보다 크다.** 슬리피지 0.1%/편을
낮추자는 제안이 나오면 이 숫자도 근거다.

### 우위는 없다

사전 격자를 **결과를 보기 전에** 정하고 본페로니를 걸었다 — 일봉 36칸(|t|≥3.20) ·
분봉 12칸(|t|≥2.87). **통과 0칸.**

- 일봉 최대 |t| = 2.42 (vol_breakout T3, **음수**). 양수 쪽 최대 2.19
- 분봉 최대 |t| = 1.62. **12칸 전부 테이커 보정 뒤 비용 전이 음수**
- 저가 대 나머지를 전략별로 합산해도 **어느 방향에서도 저가가 낫지 않다**

**거울이 여기서도 결정적이다.** 분봉에서 mean_reversion +1.85틱 / 거울 −2.01틱,
ma_cross −1.96틱 / 거울 +1.09틱 — 부호만 뒤집히고 크기가 같다. **07-31 밤에 L
티어에서 잡은 것과 같은 현상**이고 원인도 같다: 호가 격자다.

**승률에 속지 말 것**: T3 mean_reversion은 785승 570패(z=5.84)에 중앙값 +1.659%인데
**평균은 −1.325%**다. 자주 이기고 크게 진다.

**표본 밖에서 안 남는다**: T3 mean_reversion 앞 +1.881%(t=3.49) → 뒤 −4.670%(t=−1.17).

### 유동성 — 저가주 하단은 매매가 안 된다

| 티어 | 일거래대금 중앙 | 거래대금 2천만 미만 봉 |
|------|------|------|
| ~499원 | **0원** | **65.1%** |
| 500~999 | 84,514,500 | 24.7% |
| 1,000~1,999 | 194,927,673 | 7.2% |

**500원 미만은 열 이유가 없다.** T3는 유동성 자체는 된다.

### 새로 잡은 함정 셋 (앞 절의 여덟 + 이것)

- **군집 표준오차** — 같은 날 여러 종목은 독립이 아니다. naive t가 최대 **7.5배**
  부풀어 있었다(−6.98 vs −2.35). naive·종목·날짜·월 넷을 다 계산해 **가장 큰 것**을 쓴다.
- **우측 절단** — 강제 청산을 끄면 없던 우위가 생긴다: T2 ma_cross·거울 −0.41% →
  **+3.69%**, T2 mean_reversion −0.07% → **+2.59%**.
- **앞을 보는 필터** — 오늘 값으로 과거 유니버스를 고르면 안 된다. 크기를 쟀다:
  같은 T3인데 중립 표본 **+10.28%** vs 오늘 싸다로 고른 표본 **−3.88%(t=−3.47)**,
  **14pp 차이**.

### 표본

일봉 **973종목 · 252,856봉 · 2025-07-07~2026-07-31(262거래일) · 매매 80,016건**.
분봉 **24종목 × 연속 8거래일 · 52,636봉 · 매매 20,354건**. KIS 1,737회, 전부 GET.
호가단위 표를 체결가 210,544개로 검산해 **99.61%가 격자 위**임을 확인했다.

### 틀릴 수 있는 지점

- **스프레드가 Roll 추정이다.** 실제가 1틱에 가까우면 T3 왕복은 0.302%로 내려가고
  저가의 비용 우위가 되살아난다 — 다만 **비용 전 우위가 0이라 결론은 안 바뀐다.**
- **생존 편향이 저가 구간에서 가장 크다.** census가 2026-07-31 상장분이라
  상장폐지·거래정지로 사라진 저가주가 통째로 빠졌다. 방향은 낙관 쪽이다.
- **500원 미만은 "없다"가 아니라 "못 쟀다"** — 매매 11~205건뿐이고 유동성도 없다.
- 절반 격자 체결 1.5%(5,000~20,000원 구간, 5원 단위)의 정체를 모른다.

### 이번에 지킨 선

주문·자동매매 **0건**. 제품 코드·리스크 룰·`.env`·`.cache/` 무변경. KIS는 GET만.

## 2026-08-11 오후 — 전 종목 21년치 일봉 저장소를 만들며 잰 것

측정 유니버스가 143~149종목(국내 활성 주식·ETF **3,923개 중 3.7%**)에 묶여 있던
이유가 저장소가 없어서였다. 잴 때마다 KIS에서 다시 받으니 폭을 늘릴 수 없었다.
`db/dailyBars.ts` + `scripts/collectDailyBars.ts`를 만들며 잰 것을 남긴다.

### ★ 어느 서버에 붙느냐가 소요 시간을 **36배** 바꾼다

같은 조회(`FHKST03010100`), 같은 종목, 같은 시각대에 잰 값이다.

| 서버 | 한 종목(21년 · 약 60회) | 같은 조회 3회 |
|------|------|------|
| 모의(vts) | **584.5초** | 0.70초 · 13.06초 · 0.53초 (들쭉날쭉) |
| 실전(prod) | **7~10초** | 0.51초 · 0.51초 · 0.53초 (일정) |

초당 한도 차이(모의 1건/초 · 실전 18건/초)만으로는 설명되지 않는다. 모의 서버는
**응답 자체가 느리고 예측이 안 된다.** 전 종목으로 환산하면 모의 26일 · 실전
13~16시간이다. 그래서 수집기는 모의 서버에 붙으면 시작할 때 이 사실을 적는다.

### KIS 일봉은 21.4년이 한계가 아니다 — 우리가 60쪽에서 끊은 것이다

`getDailyCandleHistory`가 130달력일씩 나눠 받으므로 60쪽 = 7,800일 = 21.4년이다.
그 너머에도 값이 있다: 005930·000020 모두 **2004-01-02**까지 정상 응답했다.
지금 저장소의 하한(2005-03-09)은 KIS의 한계가 아니라 `--pages` 기본값이다.

### 수정주가는 요청 시점 기준이고, 경계는 실제로 이어진다

005930 5,287봉(2005-03-09~2026-08-10)에서 **|전일 대비| 30% 이상인 봉이 0개**였고
최대 변동은 26.8%였다. 2018-05-04 50:1 액면분할 경계도 이어진다.

| 거래일 | 종가 | 거래량 |
|------|------|------|
| 2018-04-27 | 53,000 | 30,310,800 |
| 2018-04-30 ~ 05-03 | 53,000 (고저 동일) | **0** |
| 2018-05-04 | 51,900 | 39,565,391 |

분할 기간의 거래정지 나흘은 KIS가 **거래량·거래대금 `0`으로** 준다. 그 0은 사실이라
그대로 담는다 — 빈 문자열이 와서 `NULL`이 되는 경우와 다르다(89,530봉 중 `NULL` 0건).

**그래서 `vintage`가 필요하다.** 위 값들은 *지금* 받았기 때문에 분할 반영본이다.
분할 전에 받아 둔 줄과 섞이면 값으로는 구별할 수 없다.

### 소켓 절단과 한도 초과가 **실제로** 났고, 재시도가 넘겼다

밤새 도는 것이라 지어낸 시나리오가 아니라 실측이다.

```
000020 2쪽 일시적 실패 (1/3) · 3초 뒤 다시 — fetch failed (other side closed)
000020 6쪽 일시적 실패 (1/3) · 3초 뒤 다시 — fetch failed (other side closed)
000050 38쪽 일시적 실패 (1/3) · 3초 뒤 다시 — … 초당 호출 한도를 넘었습니다 (EGW00201)
```

셋 다 다음 시도에서 통과했다. **한 종목이 60쪽이라 페이지마다 재시도해야 한다** —
종목 단위로 재시도하면 60쪽 안에 한 번이라도 끊기는 종목은 영영 끝나지 않는다.

### 저장 규모 (실측 24종목에서 환산)

24종목 89,530봉이 **15 MB**(봉당 168바이트). 종목당 평균 3,730봉인데 이건
종목코드 앞쪽(오래된 KOSPI 종목)이라 과대추정이다. 전 종목이면 **1.5~2.5 GB** 어림.

### 확인하지 못한 것

- **분할 시 거래량도 조정되는가.** 대조는 시·고·저·종가만 본다. 값이 바뀌면 어차피
  잡히므로 기준 혼입은 놓치지 않지만, 거래량만 조정되는 경우가 있는지는 안 재 봤다.
- **60쪽 상한을 넘긴 종목이 실제로 몇 개인가.** 24종목 중 **15개**가 상한
  (`oldest_day = 20050309`)에서 멈췄다 — 전 종목 비율은 다 받아 봐야 안다.
- **상장폐지·장기 거래정지 종목.** 최근 130일에 봉이 0건이면 대조를 못 하는데,
  둘을 가르지 않고 사유만 남긴다(저장분은 지우지 않는다).

## 2026-08-14 — 생존편향을 처음으로 걷어 냈다. 그리고 판정문이 하루 만에 낡았다

전날 폐지 종목 봉 913개를 받아 놓고 끝냈는데, **그것이 측정에 닿는 길이 두 군데
막혀 있었다.** 받는 것과 쓰는 것은 다른 문제였다.

### 문 두 개 — 받아 놓고도 안 쓰이고 있었다

**① 정리매매를 수정주가 파탄으로 삼켰다.** 정리매매에는 가격제한폭이 없다.
`scanAdjustmentBreaks`는 한계를 넘는 봉을 "다른 자로 잰 값"으로 보고 **마지막
파탄 이전을 통째로 버리는데**, 폐지 직전 −60% 한 봉이 그 종목의 21년을 지웠다.
생존편향을 걷으려고 받은 표본이 걷는 그 자리에서 다시 빠진 것이다.

면제 폭 7봉은 실측으로 정했다. 계열이 끝난 종목의 한계 초과가 마지막 봉으로부터
몇 봉 앞에 있는지 세어 보면:

| 끝에서 | 파탄 건수 | 종목 | 하락 비율 |
|---|---|---|---|
| 1~7봉 | 2,180 | 526 | 82.7% |
| 8~15봉 | **2** | 2 | 100% |
| 16~30봉 | 12 | 8 | 33.3% |
| 31~120봉 | 33 | 27 | 24.2% |
| 120봉 초과 | 301 | 125 | 42.2% |

**7과 8 사이에 절벽이 있다.** 정리매매 기간이 7거래일이라는 규정과 실측이 같은
곳을 가리킨다. 120봉 초과의 301건은 상승·하락이 반반이라 액면분할·병합 쪽이고,
그건 지금처럼 계속 파탄으로 잡아야 한다.

★ **면제된 봉은 계열에서 빼지 않는다.** 그 −60%가 곧 재려던 폐지 손실이다.

**② `loadEligibleSymbols`가 `is_active = true`로 걸렀다.** 폐지 종목은 그 표에서
비활성이라 봉이 있어도 패널에 못 들어왔다. 조건을 빼기 전에 무엇이 들어오는지
세어 봤다:

| | 최근 봉 있음 | 최근 봉 없음 |
|---|---|---|
| `is_active = true` | 2,767 | 3 |
| `is_active = false` | **0** | 803 |

`is_active=false`인데 봉이 이어지는 종목이 0개라, 문을 열어도 번지지 않는다.

### ★ `is_active`는 폐지 여부의 답이 아니다

코스닥→코스피 **이전상장이 KIND에 상장폐지로 기록된다.** 그 종목들은 오늘도
같은 종목코드로 거래된다 — 신세계푸드·LG유플러스·엘앤에프·포스코DX·비에이치.
스팩소멸합병도 마찬가지다.

폐지 기록 1,287건 중 **131건이 마스터에 살아 있고**, 사유 글로도 안 갈린다:
그중 66건은 사유가 이전상장이 아니었다(`지주회사의 완전자회사화`, `해산 사유
발생`, `유가증권시장 상장`…). 반대로 메리츠화재(000060)는 계열이 2023-01-27에
끊겼는데 마스터에는 활성으로 남아 있다.

**계열이 끊겼는지는 봉이 말한다.** 그래서 셋으로 가른다:

```
covered     봉이 있고 계열이 끝났다      → 표본에 실제로 반영된 퇴장
missing     봉이 아예 없다               → 아직 남은 편향
continuing  봉이 있고 계열이 이어진다    → 퇴장이 아니었다
```

### 판정문 맨 위가 하루 만에 거짓말이 됐다

생존편향의 크기가 `KIND_DELISTING_GAP` 상수로 박혀 있었다(8/13에 잰
`missingSymbols: 840` · `barsCollected: false`). 그날 밤 803종목이 들어왔는데
**맨 위는 계속 "840종목이 종목코드조차 없다"고 적었다.** 맨 위에 찍자고 만든
문장이라 더 나쁘다 — 읽는 사람이 "편향은 그대로"라고 믿는다.

DB에서 재게 했더니 **크기만이 아니라 방향까지 틀렸던 것**이 드러났다:

| 연도별 누락률 | 상수(8/13) | 실측(8/14) |
|---|---|---|
| 2005 | 35.2% | 5% |
| 2010 | 26.4% | 0% |
| 2018 | 8.8% | 42% |
| 2025 | — | 66% |

옛날이 많이 빠진 줄 알았는데 **최근이 많이 빠져 있었다.** 그리고 그 원인이
데이터가 아니라 **수집이 완주하지 못한 것**이었다 — 커서를 보니 218종목이 아예
시도조차 되지 않은 채 끝나 있었다(로그도 `/tmp`와 함께 사라져 몰랐다).

다시 걸어 9분에 178,734봉이 들어왔다. **누락률 21.9% → 1.5%**
(covered 1,017 · missing 15 · continuing 107).

0봉으로 온 7종목은 2005-03 이전에 실질 거래가 끝난 것들이다. 000110은 KIS가
75봉을 주는데 전부 거래량 0이라(거래정지 상태로 폐지) `trimTrailingZeroVolumeBars`가
전부 뗐다 — **값이 없는 게 아니라 거래가 없었다.**

### 편향 보정 전후 — 신호는 살아남았고, 5~18% 줄었다

같은 절차(expanding · 축 고정 · 비용 0 · 진입 익일 시가 · 진입 3,836건):

| 축 | 편향 있음(2,654종목) | 폐지 포함(3,666종목) | 차이 |
|---|---|---|---|
| 1일 | +34.39% · t +5.49 | **+33.46% · t +5.46** | −0.93%p |
| 3일 | +17.74% · t +4.62 | **+16.13% · t +4.62** | −1.61%p |
| 5일 | +10.03% · t +3.20 | **+8.26% · t +2.71** | −1.77%p |
| 10일 | — | −2.90% · t −1.03 | |
| 20일 | — | −2.64% · t −0.90 | |

위약 20가족 |t| 95분위 1.04 · 최대 1.40.

★ **그런데 손익분기표의 답은 그대로다.** 생존편향을 걷어도 비용을 넘는 축이
하나도 없다 — 가장 가까운 3일 축이 왕복당 +0.192%인데 비용 0.30%면 −0.108%다.

| 축 | 왕복/년 | 총우위/왕복 | 95% CI(해 군집) | 0.30% 대비 | 0.54% 대비 |
|---|---|---|---|---|---|
| 1일 | 252.0 | +0.133% | [+0.085, +0.180] | −0.167% / −42.1% | −0.407% / −102.6% |
| 3일 | 84.0 | **+0.192%** | [+0.111, +0.273] | −0.108% / −9.1% | −0.348% / −29.2% |
| 5일 | 50.4 | +0.164% | [+0.056, +0.272] | −0.136% / −6.9% | −0.376% / −19.0% |
| 10일 | 25.2 | −0.224% | [−0.377, −0.071] | −0.524% / −13.2% | −0.764% / −19.3% |
| 20일 | 12.6 | −0.530% | [−0.840, −0.221] | −0.830% / −10.5% | −1.070% / −13.5% |

**정보는 있고, 그 정보로는 돈이 안 된다.** 어제와 같은 결론이지만 이제
생존편향 위에서 내린 것이 아니다.

### ★ 같은 계산을 두 번 세어 스스로 문턱을 올렸다

확장창과 이동창을 따로 돌렸다. **학습 구간은 분명히 다른데**(이동창은
20060804~, 20070730~ …) 축 1·3·5일에서 **15/15창 모두 `reversal5 1일`을 골라
검증 결과가 소수점 5자리까지 같았다**(33.46302 · 16.13066 · 8.26294).

그런데 원장은 그것을 10칸으로 세어 본페로니 문턱을 **2.58 → 2.81**로 올렸고,
축 5일(t +2.71)이 `넘었다`에서 `넘지 못했다`로 뒤집혔다 — **값은 하나도 안
바뀌었는데 판정만 바뀐 것이다.**

`selection_signature`를 원장에 남겨 접는다. 서명은 창마다 고른 (신호·축)과
**검증 구간**으로 만들고 **학습 구간은 일부러 뺀다** — 결과를 정하는 것은
무엇을 골라 어느 구간에서 쟀는가이지 어디서 배웠는가가 아니다. 서명이 없는 옛
줄은 `id`로 갈려 각자 한 칸으로 남는다(지난 기록의 뜻을 나중에 바꾸지 않는다).

★ **접는 자리가 두 군데다.** 원장을 셀 때(`cumulativeMeasuredCells`)만 고쳤더니
반쪽이었다 — 문턱은 **남기기 전에** 계산되므로 그 실행의 판정은 여전히 10칸
기준 2.81을 썼다. 그래서 이번에 낸 서명 중 원장에 이미 있는 것을 미리 빼도록
(`countKnownSignatures`) 함께 고쳤다. 고친 뒤 이동창 실행:

```
지금까지 5칸 → 이번 5칸 중 새것 2칸을 더해 7칸 · 본페로니 문턱 |t| > 2.69
  축 5일 · 판정 t +2.71 · 본페로니 2.69 넘음 → ★ 넘었다
```

★ 이건 8/13에 이미 적어 뒀던 함정이다(*"확장·이동이 15/15창 같은 칸"*).
적어 두고 그대로 밟았다.

### 검사가 매일 갱신할 때마다 멈추고 있었다

`verifyDailyBars` 2번이 **vintage가 섞이면 무조건 fatal**이었는데, 그게
`collectDailyBars --refresh`의 설계와 어긋나 있었다. 증분 갱신은 최근 130일을
다시 받아 저장분과 대조하고(`compareDailyBars`) 어긋나면 통째로 다시 받는다.
어긋나지 않으면 새 봉만 덧붙이므로 **vintage가 섞이는 것이 정상 동작**이다.

혼입은 세기만 하고, 멈추는 것은 **경계에서 기준이 갈린 흔적**이 보일 때로
바꿨다. 실측: 경계 3,868건 중 한계 초과 **0건** · 평균 절대수익률 1.97% ·
최대 30.00%(상한가). 섞였지만 갈리지는 않았다.

★ **이 검사가 못 잡는 것도 적었다** — 무상증자 5% 같은 작은 수정은 가격제한폭
안이라 안 걸린다. 그쪽은 `--refresh`의 겹침 대조가 잡는 몫이다.

### 확인하지 못한 것

- **ETF·우선주 폐지는 KIND 목록에 없다.** 크기조차 모른다. 지금 `covered 1,017`은
  KOSPI/KOSDAQ 보통주 기준이고, ETF 층(포트폴리오의 50%)의 폐지 편향은 안 쟀다.
- **아직 빠진 15종목**의 성격. 사유 상위가 피흡수합병·지정자문인 계약 해지라
  정리매매 구간이 없을 수도 있지만, 세어 보지 않았다.
- **continuing 107종목이 정말 전부 이전상장·합병인지.** 봉이 이어진다는 사실만
  확인했고 사유를 하나씩 대조하지는 않았다.

## 2026-08-14 오후 — 목록을 못 구하면 질문을 바꾼다 (ETF 해지 위험)

### KIND 상장폐지 목록에는 ETF도 우선주도 없다

`searchDelCompanySub` — **회사** 폐지 목록이라 펀드인 ETF가 애초에 대상이 아니다.
실측(1,267건 전수):

| | 건수 |
|---|---|
| ETF로 보이는 이름 | **0** (`파워`가 든 일반 종목 8건이 잡혔을 뿐) |
| 코드 끝자리가 `0`이 아닌 종목(우선주) | **0** |

KRX 정보데이터시스템(`data.krx.co.kr`)은 세션을 요구해 스크립트로 못 받는다
(`getJsonData.cmd`가 `LOGOUT`을 돌려준다).

### 그래서 질문을 바꿨다

**측정 유니버스에는 ETF가 없다** — `loadEligibleSymbols`가 `asset_type = 'stock'`
으로 거르므로 walk-forward에는 ETF 생존편향이 **0**이다. 그러면 ETF 편향은
어디에 남는가? **종목 선택**이다. 포트폴리오의 50%가 ETF이고, 8/11에 고른
5종목은 *오늘 존재하는 ETF만* 보고 고른 것이다.

"얼마나 많이 사라졌나"(목록)를 못 구하면 **"내 것이 사라질 위험이 있나"**(순자산)를
재면 된다. 후자가 실제로 돈이 걸린 질문이고, 이쪽은 KIS가 답한다 —
`FHPST02400000`의 `etf_ntas_ttam`.

### 실측 — 기준선의 307~5,087배였다

KRX 규정상 ETF는 **순자산총액 50억원 미만**이 지속되면 관리종목을 거쳐 해지된다.

```
  종목    이름                        순자산  기준선대비  상장일     운용사
  329200  TIGER 리츠부동산인프라     1.53조원      307배  20190719   미래에셋자산운용
  161510  PLUS 고배당주              2.38조원      476배  20120829   한화자산운용
  411060  ACE KRX금현물              4.12조원      823배  20211215   한국투자신탁운용
  360750  TIGER 미국S&P500          20.80조원     4159배  20200807   미래에셋자산운용
  069500  KODEX 200                 25.43조원     5087배  20021014   삼성자산운용(ETF)
  운용사 미래에셋 2 · 한화 1 · 한국투자신탁 1 · 삼성 1
```

**해지 위험은 이 표본에 없다.** 걱정할 것이 아니었고, 그 사실을 재서 확인했다.
`backend/src/scripts/checkEtfRisk.ts`로 남겨 분기 리밸런싱마다 다시 묻는다.

### 만들면서 밟은 것

- **ETF 현재가 응답에 종목명이 없다**(58필드). `mbcr_name`=운용사,
  `etf_rprs_bstp_kor_isnm`=기초지수뿐이라 이름은 `instruments`에서 가져온다.
- `etf_ntas_ttam`은 **억원 단위**다. 상장좌수 × 현재가로 검산해 확인했다
  (069500: 236,250,000 × 109,100 = 25조 7,700억 ≈ 254,325억).
- **모의 서버는 초당 1건**이라 300ms 간격에서 5종목 중 1종목이 `초당 거래건수
  초과`로 실패했다. 그런데 판정문은 남은 4종목만 보고 *"가장 작은 것도 476배"*라고
  적었다 — **못 받은 것이 가장 작을 수도 있다.** 실패가 있으면 판정을 하지 않게 고쳤다.
- `APP_ENV=prod`로 돌리니 잔고 조회가 `EGW02004`로 막혔다(모의 앱키를 실전
  도메인에). **가드가 설계대로 동작한 것**이다 — 이 TR은 모의 서버에도 있어
  그냥 모의로 돌리면 된다.

### 확인하지 못한 것

- **과거에 해지된 ETF가 몇 개인지는 여전히 모른다.** 지금 답은 "내 것은
  안전하다"이지 "ETF 해지가 드물다"가 아니다. 소규모·테마 ETF는 실제로 해지된다.
- 운용사 쏠림이 몇 개까지 괜찮은지. 세어서 적기만 한다.
